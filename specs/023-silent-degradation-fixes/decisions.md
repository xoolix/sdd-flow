# Decisions

[2026-08-28] R-007 (spike de OpenSpec) recomienda NO adoptar el paquete y robar la idea: capa de spec viva propia alimentada por /archive-feature, sin tocar bin/sdd. Consecuencia para 023: los SEIS arreglos sobreviven, no cuatro. Corrige la lectura previa del orquestador, que había dado el item de sdd-designer por trabajo tirado asumiendo adopcion.
[2026-08-28] El item de "apendear deltas a plan.md" queda FUERA de 023 (ver clarify.md): en el modelo de referencia design.md se congela a proposito. La capa de spec viva es feature MEDIUM aparte.
[2026-08-28] spec.md quedó en 763 palabras contra un presupuesto de 650. Quinto artefacto consecutivo por encima (020: 665, 021: 661, 022: 831, 023: 763) — el judge del ciclo 3 de 022 ya lo había marcado como evidencia de calibración mala para features cargados de contrato, no como desliz por feature. Se registra en vez de recortar: los 8 AC y la sección Unchanged son contenido pedido explícitamente por el usuario en el quality gate.
[2026-08-28] Lane: full-spec. `/new-fix` había arrancado por el ítem de la base del PR solamente; con "fixiemos todo" pasan a ser seis ítems en CLI + agentes + skills + tests, o sea >2 GWT y >1 dominio ⇒ escalado a full-spec según la regla de la propia skill.
[2026-08-28] RECORTE DE ALCANCE: el usuario sacó `cmd_open_pr` del feature ("no abras el pr, lleguemos hasta los commits"). AC2 y AC3 quedan fuera; la numeración de los AC NO se renumera, se marcan como fuera de alcance — 022 acaba de pagar el precio de renumerar y dejar 24 citas colgando. AC1 se queda pese a sonar a PR: `cmd_base_branch` es de donde `/simplify-code` (`sdd-simplify-code.md:21,44`) y `sdd-cross-reviewer` (`:34`) sacan el rango del diff, así que resolver post-archive es del lado de los commits.
[2026-08-28] El recorte DISUELVE el único hallazgo de alto impacto del discovery checkpoint (conflicto entre AC2 pidiendo procedencia y el guardarraíl que prohíbe tocar el stdout de `sdd base-branch`). No se resolvió: desapareció junto con su causa.
[2026-08-28T11:49:49Z] implemented-by: claude
[2026-08-28T12:16:14Z] T002 review fix cycle 1 (coordinator, live repro against committed 9709496): the two-independent-toggle model (my own earlier reading of "two independent toggles for ``` and ~~~") had two defects. (1) The heading match (`$0 == heading`) was not fence-gated like the terminator was, so a heading-shaped line INSIDE a fence was accepted as the real section start — worse than the original bug, since it hands a caller fabricated example content at exit 0 instead of just cutting real content short. (2) Two independent per-character booleans don't model how fences nest: CommonMark closes a fence only with a fence of the SAME character, so a literal `~~~` inside an open ``` block is content, not a delimiter — treating it as an independent toggle let it flip on and never flip back off, permanently gating the terminator. Corrected to a single `fence` variable keyed to the delimiter that opened the current fence (`""` when closed), gating BOTH the terminator and the heading match on `fence == ""`.
[2026-08-28T12:16:14Z] Corrected 3 of the 13 T002 tests that had encoded the same wrong assumption (a heading-shaped line inside a fence counts as the real section start): both "fence opened above the heading" tests (`cmd_domain_vocab`, `build_pr_body_file`) now assert the fenced occurrence is NOT recognized — with no real section elsewhere, nothing is found (exit 3 / empty), not that content flows through. The `build_pr_body_file` "unclosed fence" test's claim that `Rollback Plan` "still resolves on its own" was also wrong for a genuinely unclosed fence: a real unclosed fence swallows the rest of the physical file for EVERY section's independent extraction call, not just the one it opened in — verified live, corrected to assert `Rollback Plan`'s own extraction comes back empty too. Added 5 new tests demonstrating both defects directly (per coordinator review), run against the flawed 9709496 binary first: 4 were red, 1 (info-string fence closed by a bare ```) already passed.
[2026-08-28T12:24:32Z] T003: index-state axis (AC5, AC8) walked as 4 explicit values — clean, staged outside the feature dir, staged inside, both at once — not just the "staged inside" shape the task title names. Run against the unmodified binary first: clean and outside already passed (existing behavior, AC9 from 022, correctly unchanged); inside and both were red for the expected reason (no warning text on stderr at all, or only the pre-existing post-commit warning). Fix: `feature_dir` (absolute, built from `"$(pwd)/specs"`) normalized to repo-relative by stripping `"$(pwd)/"` before prefix-matching against `pre_staged` (repo-relative) — confirms F8's live repro (a naive match never fired). Kept as a second, independent check sharing only the `pre_staged` snapshot with the existing post-commit safety net: this one is an inclusion set scoped to the feature dir, checked before staging; that one is an exclusion set over the whole index, checked after the commit. The "both at once" test reuses the existing post-commit-hook trick (dirtying a committed file from inside `.git/hooks/post-commit`) alongside a pre-staged feature-dir file, and asserts both warnings appear with their own distinct text — proving the two checks did not get merged into one. No `git add` call changed; all three stay `git add -- "$path"`. 4 new tests, full suite 123 → 127, all green.

## Simplify: 2026-08-28 — /simplify-code
- **Files simplified**: none
- **Changes**: Scope resolved to 1 file after exclusion filters (`bin/sdd`) out of 13 in the `integration/sdd-020-021..HEAD` diff — `.claude/agents/**/*.md` (2 files), `specs/**/*.md` (6 files), and `tests/sdd.test.js` were excluded by the current list; `research/R-007-openspec-adoption/{research.md,prior-interview.md}` (2 files) were treated as excluded by the same prose-artifact reasoning even though `research/**/*.md` is not yet on the exclusion list (flagged, not added — that's a separate fix); `specs/archive/2026-08-26-022-pipeline-integrity-fixes/.pr-opened` (1 file, a plain key:value PR record for an already-archived, unrelated feature) was treated as out of scope on the same non-code-data-record reasoning, a second gap in the same list. Reviewed `bin/sdd`'s three touched functions (`cmd_base_branch` Layer 1 / `resolve_feature_dir`, `cmd_commit_slice`'s new pre-staged warning block, `extract_section`'s fence-aware rewrite) against KISS/DRY/YAGNI: no changes applied. All three are minimal, already-reviewed code (T002 alone went through two fix cycles catching two live defects) with load-bearing comments documenting non-obvious invariants; the two warning checks in `cmd_commit_slice` were left unmerged per explicit instruction (opposite polarity, different timing). No edit made.
- **Baseline**: pass (128/128) | **Post-edit**: pass (128/128, no code changed)

## JUDGMENT-DAY-HIGH — 023-silent-degradation-fixes

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | `extract_section`: el regex de apertura de fence ancla en columna 0 (`/^```/`, `/^~~~/`), así que un **fence indentado** —el que aparece adentro de un item de lista— es invisible al tracker. Si adentro hay una línea con forma de `## `, el gate no se activa, la sección corta ahí y el contenido posterior se pierde con exit 0. **Reproducido por el judge y por mí.** No es hipotético: **12 archivos `.md` de este repo** usan fences indentados (`sdd-simplify-code.md`, `sdd-reviewer.md`, `sdd-cross-reviewer.md`, varios SKILL.md). Es el defecto que T002 existe para cerrar, en un valor del eje "estructura del documento" que ni AC4 ni la tabla de ejes del plan enumeraron. Contradice el Success Criterion del propio feature. |
| 2 | medium | `extract_section` normaliza cualquier fence de largo ≥3 a un string fijo de 3 caracteres, así que un fence de 4 backticks —que CommonMark define justamente para anidar ejemplos que contienen triple-backtick— se cierra con el backtick triple interno. Reproducido. Baja probabilidad hoy: ningún `.md` del repo usa fences de 4+. |
| 3 | medium | Las dos carpetas legacy (`003-plan-discovery-checkpoint`, `004-adversarial-review-agent`) **nunca** resuelven por el glob de `resolve_feature_dir`, porque su nombre ES el feature-id sin prefijo. Hoy es inocuo solo porque ninguna tiene `.parent-branch`. Si alguna lo tuviera, `cmd_base_branch` caería en silencio a la capa 2/3 — la misma forma de degradación que AC1 arregla. El hueco no está registrado como riesgo aceptado en ningún lado. |
| 4 | medium | **Respuesta a la pregunta que se le hizo sobre AC8**: la disciplina cambió la *organización* (ahora un hueco se nombra como "falta este valor del eje" en vez de "falta esta forma de bug") pero **no evitó shipear un defecto**. T002 shipeó con 13 tests verdes sobre ese mismísimo eje y dos defectos vivos; después del arreglo, con 18 tests, apareció un tercer valor sin enumerar (el #1 de arriba), encontrado otra vez por repro manual. La causa: la lista de valores del eje salió del brainstorm de los autores, no de un oráculo externo. "¿Está cubierto el eje?" no tiene hoy contra qué contrastarse. |
| 5 | low | El guard de `resolve_feature_dir` funciona porque los cuatro llamadores lo invocan como condición de `if`, pero el docstring de la función no dice que sea obligatorio. Un llamador futuro con assignment pelado abortaría el script bajo `pipefail`, sin texto de error. Una línea de comentario lo previene. |

Verdict del judge: FAIL (por el #1).
Source: sdd-judge, review-feature phase
Date: 2026-08-28

[2026-08-30] JUDGMENT-DAY-HIGH RESOLUTION: el usuario eligió **arreglar contra CommonMark**, no parchear el caso que se rompió. Alcance: fence indentado (#1) y fence de largo ≥4 (#2) juntos, derivando los valores del eje "estructura del documento" de la gramática de fenced code blocks de CommonMark —apertura de 3+ backticks o tildes; indentación de hasta 3 espacios; cierre del mismo carácter con largo ≥ al de apertura; indentación del cierre puede diferir; info string solo en la apertura— en vez de seguir agregando el caso que acaba de fallar. Registrar el riesgo de las carpetas legacy (#3) en las Edge Cases del spec y limpiar los comentarios obsoletos de `tests/sdd.test.js` que todavía describen el modelo de dos toggles descartado.

Motivo, del hallazgo #4 del judge: es la TERCERA vuelta sobre `extract_section` en dos features, y las tres veces el hueco fue un valor del eje que nadie enumeró. La lista salió del brainstorm de los autores, no de un oráculo externo. Derivarla de la gramática le da a "¿está cubierto el eje?" algo contra qué contrastarse, que es lo que AC8 prometía y no tenía.

[2026-08-31T01:06:24Z] T002 review fix cycle 1, implementación (JUDGMENT-DAY-HIGH #1 y #2): `extract_section` reescrita contra la gramática de fenced code blocks de CommonMark en vez de parchear los dos casos reproducidos por el judge. Estado nuevo: `fence_char` ("`", "~", o "" cuando está cerrado) + `fence_len` (largo del delimitador de apertura), en vez del string fijo de 3 caracteres anterior. Apertura: `match($0, /^ */)` mide la indentación (RLENGTH), se descarta como fence si son 4+ espacios (CommonMark: eso es un bloque de código indentado, no un fence — decisión deliberada, sin protección de heading adentro), y el resto de la línea se testea con `match(rest, /^`+/)` o `/^~+/` para medir el largo real de la corrida vía RLENGTH (no `{n,}` — `match`/`RLENGTH` es "la forma natural de medir una corrida de delimitador" con awk POSIX puro, sin depender de interval expressions). Cierre: mismo carácter, indentación propia ≤3 espacios evaluada independientemente de la apertura, corrida recortada de espacios finales (`sub(/ +$/, "", closer)`) que debe ser TODA del carácter del fence y de largo ≥ `fence_len` — así una corrida más larga cierra (4 backticks cierran un fence de 3) y un info string en el cierre (` ```js `) no cuenta como cierre real. El heading match y el terminador siguen gateados en `fence_char == ""`, sin cambios de esa parte (ya era correcta desde el ciclo previo).

Tests: 16 casos nuevos (8 por consumidor × `cmd_domain_vocab` y `build_pr_body_file`), corridos primero contra el binario sin arreglar. 12 de 16 salieron RED por el motivo esperado (indentado 1/2/3 espacios no trackeado; fence de 4+ backticks/tildes cerrado por una corrida interna de 3; cierre indentado distinto de la apertura nunca reconocido; un cierre con info string cerraba igual). 4 ya pasaban — correcto y esperado bajo AC8: el marcador indentado 4 espacios ya no abría fence bajo el código viejo tampoco (mismo resultado, por el motivo correcto una vez que la aserción se corrigió — ver nota de fixture abajo), y un cierre más largo que la apertura ya cerraba por accidente (el matching viejo era `index($0, fence) == 1`, un prefix-match que no exigía longitud exacta). Dos de los 16 tests tuvieron un defecto de fixture propio detectado antes de fijar el RED real: la versión de `cmd_domain_vocab` de "cierre más largo que la apertura" etiquetaba como "should not appear" una línea que en realidad es contenido real post-cierre (se corrigió el fixture, no el código); y tres tests de `build_pr_body_file` (cierre indentado distinto, cierre con info string, mirror de tilde con info string) usaban solo `body.toContain` sobre el cuerpo ensamblado, que "pasa" aunque el fence nunca cierre de verdad porque la sobre-inclusión de AC hacia adelante arrastra el marcador de Rollback Plan igual — se reescribieron con `extractSectionViaRealPath` directo sobre AC y Rollback Plan por separado (mismo patrón que ya usaba el archivo) para que el test dependa de que el cierre ocurrió, no de que el contenido aparezca en algún lado del cuerpo. Con eso, 12 RED reales / 4 PASS reales confirmados antes de tocar `bin/sdd`; los 16 en verde después, suite completa 128 → 144, sin regresiones.

Limpieza: dos comentarios en `tests/sdd.test.js` (líneas originales ~2032 y ~2936, ambos con el texto "F9... dos toggles independientes") todavía describían el diseño de dos toggles descartado en el ciclo anterior como si fuera el mecanismo vigente. Corregidos para describir el estado real (`fence_char`/`fence_len` único, cierre solo con el mismo carácter). Riesgo de las carpetas legacy (#3 del judge) registrado en `spec.md` → Edge Cases como riesgo aceptado, sin tocar `resolve_feature_dir`.

## Simplify: 2026-08-31 — /simplify-code
- **Files simplified**: none
- **Changes**: Segunda pasada. Único commit nuevo desde la pasada anterior (`4356716` → `adaea77`, "T002 review fix cycle 1: derive fence grammar from CommonMark") tocó 4 archivos: `bin/sdd`, `decisions.md`, `spec.md`, `tests/sdd.test.js`. Confirmado por `git show --stat adaea77` que ninguno de los otros 9 archivos del diff de scope (`.claude/agents/sdd-designer.md`, `.claude/agents/sdd-simplify-code.md`, `research/R-007-openspec-adoption/{research.md,prior-interview.md}`, `specs/023-silent-degradation-fixes/{clarify,discovery,plan,tasks}.md`, `specs/archive/2026-08-26-022-pipeline-integrity-fixes/.pr-opened`) cambió — son byte-idénticos al estado ya revisado en la pasada del 2026-08-28, así que ese juicio ("prosa, no código", más las dos brechas ya señaladas de `research/**/*.md` y `.pr-opened`) se lleva sin re-derivar. Scope tras filtros: 1 archivo (`bin/sdd`). Dentro de ese archivo, el único código nuevo es la reescritura de `extract_section` (T002 review fix cycle 1, tercer diseño de la función en dos features): indentación medida con `match($0, /^ */)`/`RLENGTH`, `rest` separado del indent, y estado `fence_char`+`fence_len` en vez del string fijo de 3 caracteres anterior. Revisado contra KISS/DRY/YAGNI: las dos ramas casi idénticas (backtick vs. tilde) en apertura y en cierre parecen candidato a DRY, pero son las únicas 2 variantes que CommonMark define (no una lista abierta) y colapsarlas en un loop con regex dinámico (`match(rest, ch "+")`) no elimina complejidad real — solo la reubica detrás de una indirección, sobre una función que ya está en su tercer diseño y cuyas dos pasadas previas enviaron defectos a producción. El comentario que ancla las reglas a la gramática de CommonMark (spec.commonmark.org §4.5) sigue intacto, sin tocar. No se detectaron variables sin uso, ramas muertas, ni nesting innecesario. No se aplicó ningún cambio: el riesgo de una cuarta regresión supera cualquier ahorro cosmético de líneas en código ya verificado en vivo contra toda la gramática (ver lista de comportamientos congelados en el prompt de esta pasada).
- **Baseline**: pass (144/144) | **Post-edit**: pass (144/144, no code changed)

## JUDGMENT-DAY-HIGH (2) — 023-silent-degradation-fixes (re-review ciclo 1)

| # | Sev | Descripción |
|---|---|---|
| 6 | **high** | Un backtick adentro del info string de una apertura con backticks es ILEGAL per CommonMark §4.5 — esa línea no abre fence. El código la acepta mirando solo el largo de la corrida, así que se traga un heading real y contenido ajeno. Reproducido por el judge y por mí. Ningún test lo cubre: los tests de "info string" existentes cubren el CIERRE, que es otro caso. |
| 7 | **high** | El trim del cierre (`sub(/ +$/, "", closer)`) saca espacios pero no **tabs**, y CommonMark permite ambos. Un cierre con tab al final no cierra, el fence queda abierto y la sección se filtra hasta EOF tragándose headings reales. Reproducido en las dos ramas (backtick y tilde). Cero cobertura: `grep -i tab tests/sdd.test.js` no devuelve nada relevante. |
| 8 | low | El #5 del ciclo anterior sigue sin atender: `resolve_feature_dir` no documenta que hay que llamarla como condición de `if` bajo `set -euo pipefail`. |

**Respuesta empírica a la pregunta sobre AC8** (el judge reprodujo las 8 cláusulas que se le nombraron; 6 salieron limpias, 2 fallaron): derivar el eje de CommonMark **reorganizó el hueco, no lo cerró**. Movió el brainstorm de "qué forma de markdown lo rompió" (ciclos 1-2) a "qué cláusulas de CommonMark nos molestamos en transcribir" (ciclo 3), y la transcripción dejó afuera dos. Quinto defecto vivo en `extract_section` en tres reescrituras a lo largo de dos features; los cinco encontrados por reproducción manual, ninguno por la lista de ejes.

Verdict del judge: FAIL.
Source: sdd-judge, review-feature phase (re-review ciclo 1)
Date: 2026-08-30

[2026-08-31] JUDGMENT-DAY-HIGH (2) RESOLUTION: el usuario eligió **sacar `extract_section` de awk** y moverlo a un helper en Node, en vez de un cuarto rediseño en awk.

Razón: cinco defectos vivos en la misma función, tres reescrituras, dos features — y los cinco comparten causa mecánica. El reviewer la nombró con precisión: el scanner en awk chequea **posición** estructural (largo de la corrida, indentación, conteo) pero no restricciones de **contenido** de la línea completa; esa es la categoría que falta, no dos valores más. En awk cada restricción de contenido hay que emularla (no hay cuantificador no-greedy, el manejo de tabs es incómodo, la cirugía de strings es a mano); en JS son operaciones normales.

Beneficio secundario que importa dado el historial de este repo: el helper se testea directo desde jest, sin `source bin/sdd`, que es la técnica que en 022 produjo tests que espejaban la implementación.

Contra aceptado explícitamente: `bin/sdd` hoy es shell puro y corre en cualquier repo sin Node; esto le agrega Node como dependencia de runtime. Node ya es dependencia de desarrollo del repo (jest, package.json).

Reviewer y judge coincidieron en FAIL por los mismos dos defectos, cada uno reproduciéndolos por separado, y el reviewer los contrastó contra el texto de spec.commonmark.org 0.31.2 §4.5 en vez de asumirlos.

[2026-08-31T10:52:51Z] T002 review fix cycle 2, implementación (JUDGMENT-DAY-HIGH (2) #6 y #7 — mover `extract_section` a Node): nuevo módulo `src/extract-section.js`, exporta `extractSection(content, headingText)` puro (sin tocar el filesystem) más un entrypoint CLI (`node src/extract-section.js <file> <heading>`) para uso directo desde jest y desde `bin/sdd`. `extract_section` en `bin/sdd` queda como wrapper de 6 líneas: resuelve `node` en PATH y ejecuta `"$SDD_HOME/src/extract-section.js"` — mismo mecanismo de resolución que el resto del archivo usa para recursos empaquetados junto al script (`$SDD_HOME/.claude/skills/...`, `$SDD_HOME/.specify/templates`), no uno nuevo.

Lógica: mismo estado `fenceChar`/`fenceLen` (uno solo, no toggles independientes) y mismo orden de gating (heading match y terminador ambos condicionados a `fenceChar === ""`) que el diseño de review fix cycle 1, con las dos correcciones de contenido que awk no podía expresar con naturalidad: (1) apertura con backticks — si el resto de la línea después de la corrida de backticks contiene un backtick, la línea NO abre fence (§4.5, "may not contain any backtick characters"); las tildes no tienen esa restricción, verificado con test de control en ambos sentidos. (2) cierre — el trim de espacio final ahora es `/[ \t]+$/` en vez de `/ +$/`, así un cierre con tab final sí cierra, en las dos ramas.

Node ausente: `command -v node` antes de invocar; si falta, `bin/sdd` imprime `error: extract_section requires Node.js...` a stderr y retorna 1 — sin fallback a awk. Decisión deliberada (dos implementaciones = dos superficies de bug, que es justo lo que este cambio elimina) y verificada en vivo: bajo `set -euo pipefail`, un `return` no-cero dentro de `$(...)` asignado a variable SÍ aborta el script (confirmado con un repro mínimo de bash), así que tanto `cmd_domain_vocab` (`section="$(extract_section ...)"`) como las tres llamadas de `build_pr_body_file` (dentro de un bloque `{ ... } > "$body_file"`, sin `|| true` en ningún caso) abortan ruidosamente con el mensaje de stderr visible en vez de continuar con un body parcial o un exit code silencioso — verificado en vivo con `PATH` sin node contra ambos consumidores reales, incluida la instalación real vía el symlink de `/usr/local/bin/sdd`.

Sobre "cmd_domain_vocab distingue sección vacía de sección ausente por exit code": verificado en vivo contra el binario real que **no hay tal distinción** — archivo ausente, heading ausente, y heading presente con contenido vacío devuelven los tres el mismo exit 3 (por diseño: `cmd_domain_vocab` sólo diferencia "hay contenido real" de "no lo hay", no la razón). Lo que sí hay que preservar — y se preservó, confirmado por la suite completa sin regresiones — es que `extract_section` nunca sale con código distinto de 0 mientras el archivo exista, sin importar si el heading aparece o no: ambos call sites dependen de eso bajo `set -e` para no abortar en el caso normal de "sección ausente".

Limpieza: el comentario extenso de gramática CommonMark que vivía sobre `extract_section` en `bin/sdd` describía mecanismo que ya no está en ese archivo — se resumió a un puntero corto y el contenido de fondo (por qué cada regla, el framing de eje-no-bug-report) se trasladó al doc comment de `src/extract-section.js`, ampliado con las dos reglas nuevas. El comentario de cabecera del segundo describe block "document structure — fenced code blocks (T002)" en `tests/sdd.test.js` todavía describía "dos toggles independientes (F9)" como el mecanismo vigente — ya obsoleto desde review fix cycle 1 y doblemente obsoleto ahora que la lógica vive en JS; corregido para nombrar el estado único `fence_char`/`fence_len` y apuntar a `tests/extract-section.test.js`/`src/extract-section.js`.

Riesgo bajo #8 (comentario de `resolve_feature_dir`): agregado, era gratis — una línea documentando que debe llamarse como condición de `if`/`while` bajo `set -euo pipefail`.

Tests: 6 regresiones nuevas en `tests/sdd.test.js` (2 para #6 — sólo backtick, sin variante tilde ya que la restricción es específica de backticks — y 4 para #7, 2 fences × 2 consumidores), corridas primero contra el binario sin arreglar (awk) y confirmadas RED por el motivo esperado antes de tocar `bin/sdd`; las 6 en verde después del cambio. Además, `tests/extract-section.test.js` (35 tests) porta la cobertura de gramática existente contra `extractSection` directamente vía `require(...)` — sin `source bin/sdd`, sin proceso bash, sin repo git temporal — más 8 tests directos de los defectos #6/#7 (incluido un control que fija, sin reclamar que sea ideal, el límite ya existente y fuera de alcance de que un tab ANTES de la corrida del cierre no cuenta como indentación). Suite completa: 144 → 185 (144 + 6 + 35), todo verde, sin regresiones. Verificado también end-to-end contra la instalación real vía `/usr/local/bin/sdd`, que en esta máquina apunta a este mismo repo.

## Simplify: 2026-08-31 — /simplify-code
- **Files simplified**: `src/extract-section.js`
- **Changes**: Tercera pasada. Único commit nuevo desde la pasada anterior (`2d8efd2` → `fc67c73`, "T002 derive fence grammar CONTENT constraints properly by moving extract_section out of awk to Node") tocó 5 archivos: `bin/sdd`, `decisions.md`, `src/extract-section.js` (nuevo), `tests/extract-section.test.js` (nuevo), `tests/sdd.test.js`. Scope tras filtros (usando la lista vigente de `.claude/agents/sdd-simplify-code.md` paso 3, que ya incluye `.claude/agents/**/*.md` y `docs/adr/**/*.md` desde T004 de este mismo feature): 15 archivos del diff `75c4a70..HEAD` menos tests (`tests/extract-section.test.js`, `tests/sdd.test.js`), specs (6 `.md` de `specs/023-silent-degradation-fixes/`), y `.claude/agents/{sdd-designer,sdd-simplify-code}.md` → quedan `bin/sdd` y `src/extract-section.js`. `research/R-007-openspec-adoption/{research.md,prior-interview.md}` y `specs/archive/2026-08-26-022-pipeline-integrity-fixes/.pr-opened` se siguen tratando como prosa fuera de alcance por el mismo razonamiento de las dos pasadas anteriores — el hueco en la lista literal (no cubre `research/**/*.md` ni registros `.pr-opened`) persiste, sin agregarlo (ver nota explícita del prompt de esta pasada: es un follow-up separado).

  `bin/sdd`: el único código nuevo desde la pasada anterior es el wrapper de `extract_section` (8 líneas de cuerpo) que ahora resuelve `node` en PATH y delega a `$SDD_HOME/src/extract-section.js`, con fallback ruidoso (sin awk) si Node falta. Ya mínimo — sin cambios. Las otras dos secciones tocadas por este feature (`cmd_base_branch` Layer 1 / `resolve_feature_dir`, el bloque de warning pre-staged de `cmd_commit_slice`) son byte-idénticas a las ya revisadas en las dos pasadas previas (confirmado por `git show --stat fc67c73`, que no las toca) — ese juicio se lleva sin re-derivar.

  `src/extract-section.js` (nuevo, ~7.8KB, cuarto diseño de esta lógica en tres features — el primero en JS): revisado línea por línea contra KISS/DRY/YAGNI. Un hallazgo real: `const indent = (line.match(/^ */) || [""])[0].length;` — el fallback `|| [""]` es rama muerta, porque `/^ */` (cuantificador `*`, cero o más) siempre matchea en la posición 0 de cualquier string, incluida la cadena vacía; `.match()` nunca devuelve `null` para ese patrón. Verificado en vivo con `node -e` contra 6 casos (`""`, `" "`, `"   x"`, `"x"`, `"\t x"`, `"    ####"`) — los 6 devuelven un array, ninguno `null`. Simplificado a `line.match(/^ */)[0].length` con un comentario de una línea explicando por qué no hace falta el fallback. Cambio de una línea, sin tocar contrato ni comportamiento (la rama nunca se ejecutaba).

  Las dos ramas casi-duplicadas por diseño (apertura backtick-vs-tilde con la asimetría del info-string; cierre backtick-vs-tilde) se dejaron intactas — mismo veredicto que la pasada anterior sobre el equivalente en awk: son las únicas 2 variantes que CommonMark define, no una lista abierta, y colapsarlas detrás de un regex dinámico por carácter no elimina complejidad real sobre una función que ya lleva 4 defectos en vivo en 3 rediseños previos. El comentario de cabecera (gramática CommonMark, framing eje-no-bug-report) se dejó intacto — es contenido de fondo trasladado deliberadamente desde `bin/sdd` en el commit anterior, no prosa especulativa. `main()`'s `require("fs")` inline no se tocó (movimiento a top-level sería puramente estético, sin reducir complejidad real).
- **Baseline**: pass (185/185) | **Post-edit**: pass (185/185)

## JUDGMENT-DAY-HIGH (3) — 023-silent-degradation-fixes (re-review ciclo 2)

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | `cmd_open_pr` llama `body_file="$(build_pr_body_file "$feature_dir")"`. **`set -e` NO propaga el fallo de una función invocada dentro de `$()`** — verificado por el judge y por mí con repro mínimo: llamada directa aborta con exit 1, la misma función vía `$()` sigue de largo con rc=0. Consecuencia: si `extract_section` falla por cualquier motivo (Node ausente, archivo ilegible — los dos reproducidos), `build_pr_body_file` devuelve **rc=0 con un cuerpo cuyas tres secciones del spec están vacías**, y `open-pr` pushea, crea el PR y escribe `.pr-opened` sin señal. Reproducido end-to-end. Contradice AC8 del propio feature: *"ningún consumidor de `extract_section` sale 0 con contenido faltante"*, y `build_pr_body_file` es el consumidor que el spec nombra explícitamente. **Lo introdujo el arreglo**: antes del movimiento a Node, `extract_section` era awk puro y no tenía este modo de fallo. Mi nota en `decisions.md` de que "ambos call sites abortan ruidosamente" es **falsa** — la verifiqué con la forma de llamada directa que usa el harness de tests (`buildPrBodyViaRealPath`), no con la forma anidada real que usa `cmd_open_pr`. |

**Respuesta del judge a la pregunta sobre el patrón, tercera vez que se la hace**: el eje de la gramática **sí se cerró** — `src/extract-section.js` enumera las reglas de §4.5 por nombre, hay 35 tests unitarios directos, y no encontró hueco nuevo en la lógica. Lo que sobrevivió es el mismo modo de falla en un dominio nuevo: se enumeraron los fallos visibles del borde nuevo, se verificó contra un harness cómodo pero **no representativo**, y se declaró cerrado el contrato. Es estructuralmente idéntico a los ciclos 1→2: cada arreglo cierra exactamente el caso reproducido, y el razonamiento de "ahora está completo" queda un nivel más angosto de lo que el contrato real exige. Secuencia: formas de markdown → cláusulas de CommonMark → modos de falla del borde de lenguaje → cuál de las dos formas de invocación se testeó.

Verdict del judge: FAIL. Este es el ciclo 2 de los 2 que permite el protocolo.
Source: sdd-judge, review-feature phase (re-review ciclo 2)
Date: 2026-08-31

[2026-08-31] REVIEW FIX CYCLE 3 — aprobado explícitamente por el usuario por encima del cap de 2 ciclos del protocolo, porque el defecto es una degradación silenciosa dentro del feature cuyo propósito entero es eliminarlas. Los cinco tasks originales (T001-T005) se quedan `[x]` — este ciclo no reabre ninguno, es un fix directo sobre el hallazgo del judge/reviewer.

[2026-08-31T11:58:41Z] Review fix cycle 3, implementación (JUDGMENT-DAY-HIGH (3) #1): arreglo íntegro dentro de `build_pr_body_file`, sin tocar `cmd_open_pr` (fuera de alcance por decisión del usuario, y no hace falta — ver mecanismo abajo). Las tres llamadas a `extract_section` corren adentro de un grupo `{ ... } > "$body_file"`; el status de ESE grupo es el del último comando (el `printf` final, que siempre sale 0), así que un fallo a mitad de grupo quedaba enmascarado. Arreglo: cada llamada captura su propio exit status con `|| extract_failed=1` (variable local, visible después del grupo porque `{ }` es un command group, no un `( )` subshell); si alguna falló, se borra el `body_file` parcial (`rm -f`, sin dejar temp huérfano) y la función devuelve 1. Con eso, `cmd_open_pr`'s `body_file="$(build_pr_body_file "$feature_dir")"` — la asignación real, no una llamada directa — aborta sola bajo `set -euo pipefail` sin necesitar ningún cambio en `cmd_open_pr` mismo: verificado que una función que hace `return` no-cero SÍ propaga a través de `$()` en una asignación, a diferencia de un fallo interno que termina en un comando exitoso (que es exactamente el bug original).

Tests: 2 nuevos en `tests/sdd.test.js`, corridos primero contra el binario sin arreglar y confirmados RED por el motivo esperado. (a) End-to-end por el path REAL de `sdd open-pr` (Node ausente de PATH, vía un `pathWithoutNode()` que mide a `pathWithoutGh()`, más un `gh` fake mínimo para pasar el pre-flight de auth/existing-PR sin depender del estado real de esta máquina) — contra el código sin arreglar, el test mostró que el push SÍ ocurría (`git ls-remote` devolvía la rama) porque `set -e` nunca abortaba; arreglado, no pushea nada y no escribe `.pr-opened`. Esto reemplaza a propósito la cobertura que antes solo pasaba por `buildPrBodyViaRealPath` (llamada directa, sin `$()`) — el harness cómodo-pero-no-representativo que dejó pasar el defecto dos ciclos de review, señalado por ambos revisores. (b) `build_pr_body_file` llamado directamente (vía un nuevo helper `buildPrBodyFileExitStatus` que usa `if body="$(build_pr_body_file ...)"` para leer `$?` sin que el `set -e` heredado mate el script de test) con `spec.md` sin permiso de lectura (`chmod 000`, caso no específico de Node) — confirma que la función misma devuelve no-cero y no imprime ningún path de body file. Además, 4 tests nuevos en `tests/extract-section.test.js` para las dos ramas de error de la CLI de `main()` (args faltantes → exit 2; archivo ilegible → exit 1) que no tenían cobertura — comportamiento preexistente y correcto, cobertura pura, sin RED (no hay bug ahí). Suite completa: 185 → 191 (185 + 2 + 4), todo verde, sin regresiones. Paridad byte-a-byte del happy path (las 6 pruebas de `build_pr_body_file caps decisions.md...` más las 54 combinaciones de fixture/heading del bloque de gramática CommonMark) confirmada intacta — el fix no toca el camino donde `extract_section` no falla.

No tocado, verificado: `cmd_open_pr`, `cmd_domain_vocab`, `build_pr_title`, la lógica de extracción de `src/extract-section.js`, `cmd_commit_slice`'s `git add -- "$path"` (feature 020).
