# Decisions

[2026-08-25T10:17:16Z] DISCOVERY-ACCEPTED: F1 — cada agente consumidor lleva una instrucción explícita de lectura de `.claude/rules/conventions.md` § Domain rules, copiando el patrón ya probado del knob `auto-commit` (grep desde el propio prompt del agente). NO se depende de carga ambiental: `CLAUDE.md:307` afirma que ocurre, pero los tres consumidores existentes leen el archivo explícitamente y `sdd-designer.md` no lo menciona en 52 líneas.
[2026-08-25T10:17:16Z] DISCOVERY-ACCEPTED: F2 — el mecanismo DIFIERE por template. spec-template y research-template se copian literalmente, así que su comentario HTML alcanza al agente. plan-template NO se copia (`sdd-designer.md:23` dice "using ... as base"), así que su guía va en `plan-feature/SKILL.md:94-104` y `sdd-designer.md`, que es donde el designer efectivamente lee. Se descartó convertir el designer a copy-then-edit: más riesgo de regresión a cambio de una simetría que nadie observa.

## Delta: 2026-08-25 — Discovery Checkpoint (pre-plan)
- **ADDED**: instrucción explícita de lectura de `conventions.md` en cada agente consumidor. El spec asumía que el enriquecimiento "simplemente ocurría"; no existe mecanismo ambiental.
- **MODIFIED**: "la guía vive en el comentario del template" → vive en el comentario para spec y research (que se copian), y en `plan-feature/SKILL.md` + `sdd-designer.md` para el plan (que no se copia).
- **MODIFIED**: el `## Summary` del spec sobrestima el problema. La evidencia de 17 specs archivados muestra ~94% de workaround: los agentes ya ignoran la taxonomía fija. El valor real es vocabulario compartido, no permiso para escribir libre. Reescribir el Summary en esa dirección.
- **REMOVED**: el edge case que exige que `sdd-reviewer` acepte la línea de descarte. `sdd-reviewer.md:43` audita la implementación (¿el código agregó logging?), no la presencia del heading en `plan.md`.

## Delta: 2026-08-25 — Plan/tasks coherence review
- **MODIFIED**: AC6 replaced. The original ("regenerate 021's plan.md; assert no fixed sub-fields, ≥3 real paths, ≥1 N/A line, <800 words") **passes 4/4 on the current plan.md before any implementation** — the designer wrote it in the new style voluntarily. Worse, 020's plan.md, written with the OLD template, also contains none of the fixed labels: the criterion tests for the absence of something no agent writes any more (consistent with F4's ~94% workaround rate). A criterion that cannot fail verifies nothing.
  **New AC6 tests the spine instead**: Given `.claude/rules/conventions.md` § Domain rules contains distinctive domain names, When a consuming agent fills an artifact's domain section, Then those exact names appear in the output; and Given the section is emptied and the artifact regenerated, Then the names are absent and the section is scan-derived. This is the only executable check of F1's read instruction — the feature's actual value — which currently has zero verification. The regeneration stays as a sanity check, not as the acceptance gate.
- **MODIFIED**: the `extract_section` test is **genuine behavioral verification**, not a regression guard. `plan.md`'s Test strategy classifies it correctly ("Genuine — build a spec.md from the changed template, run the real extract_section path"); `tasks.md` T005 downgraded it to "guards against accidental deletion, not new coverage". The plan's classification wins: it builds a real file and runs the real parser.
- **ADDED**: coverage gap — `plan.md` names FOUR consumers for the F1 read instruction (`sdd-designer.md`, `sdd-research-spike.md`, `new-feature/SKILL.md`, `plan-feature/SKILL.md`) but the task graph wires only three (T002/T003/T004). T007 touches `plan-feature/SKILL.md` for its "Fills in:" list only. Either the instruction is added there too, or the plan should drop it from the list — `plan-feature` is the orchestrator and the designer does the filling, so it plausibly does not need it. The task graph must state which.

[2026-08-25T10:34:28Z] implemented-by: claude

## Delta: 2026-08-25 — Task T008 verification approach
- **MODIFIED**: T008 cannot literally "run sdd-designer (via plan-feature) and assert those names appear in plan.md" — `sdd-designer` is a prompt-driven LLM agent, not code reachable from Jest. The full happy path (an agent reading `conventions.md` § Domain rules and writing those names into a filled artifact) is verified only by a human running `/plan-feature` end-to-end, not by an automated test. Recorded here rather than silently downgrading the check.
- **ADDED**: the strongest real check built instead — `tests/sdd.test.js` sources the real `extract_section` (`bin/sdd:905-912`, the same heading-delimited-section parser `build_pr_body_file` already uses for spec.md, and T005's technique) and runs it against (a) this repo's real `.claude/rules/conventions.md`, asserting the § Domain rules content just written is genuinely extractable past the comment, and (b) a temp file with the section emptied back to the template comment, asserting extraction genuinely returns nothing. This proves the precondition/postcondition every consumer's "Content past the comment ⇒ use that vocabulary; empty ⇒ derive from your own scan" instruction depends on, without fabricating an agent invocation. Consumer wiring itself (all four: `sdd-designer.md`, `sdd-research-spike.md`, `new-feature/SKILL.md`, `plan-feature/SKILL.md`) is already covered by T002/T003/T004/T007's tests — not duplicated here.
- **ADDED**: `.claude/rules/conventions.md` § Domain rules filled with this repo's own functional domains (CLI surface, Phase agents, Orchestration skills, Artifact templates, Rules layer, Test suite) — named as functional areas, not directory listings, per the plan's F10 risk note. This file doubles as SDD_HOME's seed copy (`bin/sdd:232-241`); `cmd_init` skips files that already exist in a target project, so existing projects are unaffected.
- **CONFIRMED**: the non-gating sanity check (021's own `plan.md`, real paths / no fixed sub-fields / ≥1 `N/A —` line / <800 words) still holds unchanged from the prior delta — no regeneration was performed since it already passes 4/4 and regenerating it would require invoking `sdd-designer` again, out of scope for this AFK slice.

[2026-08-25T11:29:55Z] SEED-CONTAMINATION: T008 filled `.claude/rules/conventions.md` § Domain rules with this repo's own domains, and a simulated `sdd init` into a fresh temp project confirmed the new project inherits them verbatim ("CLI surface — bin/sdd subcommands", "Phase agents — .claude/agents/sdd-*.md"...). Root cause is F10: that file is simultaneously this project's copy AND the seed `cmd_init` copies (`bin/sdd:232-241`). The plan's risk mitigation only covered EXISTING projects ("cmd_init skips existing files"); it never covered new ones. The bug is latent for the whole file, not just Domain rules — `## Stack`, `## Naming`, `## Folder structure` and `## Lint / Format` are empty here only because `/init-project` was never run in this repo, and `model-overrides.md` already carries a repo-specific haiku override that would leak the same way. **This repo cannot describe itself without poisoning the seed.**
Human decision: split the seed. `cmd_init` stops copying from `.claude/rules/` and copies from a pristine versioned seed instead, so SDD_HOME can describe itself freely. Implemented as a new slice T009.

## Delta: 2026-08-25 — Task T009
- **ADDED**: `.specify/templates/rules/` — a pristine, versioned seed (`conventions.md`, `model-overrides.md`, `testing.md`, `git.md`). `cmd_init`'s rules-copy loop (`bin/sdd`) now reads from this seed instead of `$SDD_HOME/.claude/rules/*.md`. `conventions.md` and `model-overrides.md` in the seed carry headings/comments only — no Domain rules bullets, no override rows. `testing.md` and `git.md` are copied unchanged: both are already pure framework policy (TDD stance/knob, commit-per-slice/auto-commit knob) with no repo-specific fill in SDD_HOME's own copies, confirmed via `git log` — neither file has ever diverged from its first-commit content.
- **CONFIRMED**: `cmd_update` and `cmd_doctor` do not need the same redirect — neither copies `.claude/rules/*` at all (rules are create-once at `init`, never synced by `update`; `doctor` only checks `conventions.md` exists). Checked via full read of both functions plus a `grep -n 'SDD_HOME.*rules'` sweep of `bin/sdd`.
- **CONFIRMED**: end-to-end verified — `sdd init` into a fresh temp project now produces a `.claude/rules/conventions.md` with none of this repo's Domain rules content and a `model-overrides.md` with no override row, while `git.md`'s `auto-commit` knob still ships unchanged. The "skip if already exists" behavior and its log messages are unchanged (verified against a project with a pre-existing `conventions.md`).
