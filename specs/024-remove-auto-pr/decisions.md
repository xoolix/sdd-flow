# Decisions

[2026-08-31] DISCOVERY: dos hallazgos altos, ambos sobre el alcance no definido del barrido de AC5. Resueltos con una sola regla — barre `bin/`, `src/`, `.claude/**`, `.specify/templates/**`, `tests/**`; excluye `docs/` y `specs/`. Principio: se protege contra instrucciones y llamadas colgadas, no contra menciones históricas. Ver `discovery.md` para las once decisiones.
[2026-08-31] spec.md quedó en 747 palabras contra 650 (corregido 2026-08-31: la entrada original decía 690, ya desactualizado contra el propio commit que la escribió — ver la corrección del reviewer más abajo). Se registra en vez de recortar: lo que excede es el razonamiento del alcance de AC5 ("protege contra instrucción colgada, no contra mención histórica"), que es justamente lo que evita que el reviewer vuelva a derivar la contradicción que bloqueó el discovery. Sexto artefacto consecutivo por encima del presupuesto en esta familia de specs.
[2026-08-31T17:18:47Z] implemented-by: claude

## Delta: 2026-08-31 — Task T002
- **MODIFIED**: tasks.md assigned editing the "git.md policy" test (T013, `git.md rewrites the never-commit policy...`) to T004's prose pass. Stripping the PR-gate lines from `git.md`/its template seed in T002 (as T002's own bullet requires) makes that test's `sdd open-pr <feature-id>`/`draft` pins false immediately — leaving it red until T004 would break "suite green after every slice". Fixed the two dead assertions in T002 instead (dropped them, added `not.toContain("sdd open-pr")`), keeping the git.md prose change and its pinning test in the same commit. AC1/AC6 scope is unaffected; T004's own list of surviving prose-pinning tests (AI-attribution comment, simplify-code `.gitignore` assertion) is untouched and still its job.
[2026-08-31] CORRECCIÓN DE AC3 (por el orquestador, antes del review): la redacción original decía "Given que Node no está en el PATH, When corre **la suite completa**, Then pasa". Es **imposible de cumplir por construcción**: la suite es jest, y jest ES Node. El reviewer solo podía marcarla incumplida o reinterpretarla en silencio — el mismo fallo de "el test espeja la implementación en vez del criterio" que costó tres ciclos en 022. Reescrita a lo que sí es verificable y a lo que el implementador construyó correctamente: un comando de `bin/sdd` que antes necesitaba Node funciona sin él, y el grep de `node|npx|src/` sobre `bin/sdd` devuelve 0. El grep es la prueba definitiva — si no hay referencias, no puede haber dependencia. Mismo ajuste en el Success Criteria.

## Delta: 2026-08-31 — Task T004
- **MODIFIED**: `sdd-archive-feature.md`'s new "print the PR gate commands" step (3.6) uses inline code spans instead of a fenced code block for the two printed commands. A fence there would have opened a second fenced block inside the span the T007 pinning test measures (`### 3.5. Commit the slice` up to the literal string `4. **Present summary**`), which asserts exactly one fenced block (`sdd commit-slice`'s own) between those two anchors. F6's `archiveStep35Line()` constraint is satisfied either way (that regex stops at 3.5's first closing fence, before 3.6 exists at all) — this second constraint is a different, pre-existing pinning test with its own tighter regex that isn't named in T004's task bullet but would otherwise start asserting on the wrong slice of prose the same way F6 describes. No requirement changed; the AC4 commands are still printed verbatim, just without a fence around them.

## Delta: 2026-08-31 — Task T005
- **ADDED**: Running the AC5 sweep for real (not just against the 7 stale `extract_section` comments T003 left, per the task brief) found four more literal hits in `tests/sdd.test.js`: `open-pr`, `.pr-opened`, and `ready-to-pr` inside T002's and T004's own regression tests (the AC1 unknown-command test, the git.md-policy assertion, both AC4 wiring tests, and the AC6 stray-sentinel test). None of these are dangling references or historical mentions — they are proofs of removal: a test can only assert "the CLI rejects `open-pr`" or "this prose no longer says `ready-to-pr`" by naming the retired string as its search target, the same self-reference the sweep's own file has. Neither spec.md, plan.md, nor discovery.md anticipated this — discovery's F2 only flagged tests that pre-existed before T002-T004 were written, not the fresh regression tests those tasks would go on to add for AC1/AC4/AC6.
- **ADDED**: Resolved with a narrow, explicit, auditable mechanism, not a path exclusion: `sdd-sweep-exempt:start`/`:end` marker comments in `tests/sdd.test.js`, wrapping only the five proof-of-removal spans (grep the marker to see all of them). The sweep (`tests/sweep-retired-symbols.test.js`) skips lines between a matched pair. This is the same shape as `SELF_PATH`'s self-reference fix, scoped to lines instead of a whole file — it does not touch `WALK_ROOTS` or widen any directory exclusion, and a mutation test (temporarily reintroducing `cmd_open_pr` into `bin/sdd`) confirmed the sweep still catches a real dangling reference. AC5's own wording ("no encuentra ninguno") is satisfied under this reading: what "ninguno" excludes is a survivor of the deletion, and a line proving the deletion happened is definitionally not one.

## Simplify: 2026-08-31 — /simplify-code
- **Files simplified**: none
- **Sidecar de base branch**: `specs/024-remove-auto-pr/.parent-branch` no existía (gitignored, anotación local — nunca se creó para esta feature). Sin él, `sdd base-branch` caía a Layer 3 (auto-detect) y resolvía `main`, cuyo `merge-base` con HEAD es `e99e333` — el commit anterior a que arrancara toda la cadena de features 019-023, porque ninguna de esas PRs llegó realmente a `main` local (`origin/main` coincide byte a byte con `main` local: `e99e333`). Un scope contra ese base habría traído ~85 archivos de seis features ya revisadas, simplificadas y archiveadas. Creado el sidecar apuntando a `cba3f9a` (el commit inmediato anterior a T001 de esta feature, confirmado con `git log --oneline -1 2a302aa^` y `git merge-base --is-ancestor cba3f9a main` → no ancestro), replicando el patrón que dejaron ver las pasadas de simplify de 022/023 en este mismo `decisions.md` (bases como `75c4a70..HEAD`: siempre el tip inmediato anterior al primer commit propio de la feature, nunca `main`). Con el sidecar, `sdd base-branch 024-remove-auto-pr` resuelve al SHA vía Layer 1 (confirmado, exit 0) y el diff cae a los 24 archivos que esta feature realmente tocó.
- **Scope tras filtros** (lista vigente de `.claude/agents/sdd-simplify-code.md` paso 3, que ya incluye `.claude/agents/**/*.md` y `docs/adr/**/*.md`): de los 24 archivos del diff `cba3f9a..HEAD`, quedan tests (`tests/extract-section.test.js` — borrado por T003 —, `tests/sdd.test.js`, `tests/sweep-retired-symbols.test.js`), specs (6 `.md` de `specs/024-remove-auto-pr/`), `.claude/skills/{init-project,sdd-auto,sdd-next}/SKILL.md`, `.claude/agents/sdd-archive-feature.md`, `.claude/CLAUDE.md`, `.specify/templates/spec-template.md`, y `docs/adr/0004-cli-does-not-open-prs.md` excluidos por los filtros vigentes. `src/extract-section.js` — borrado por T003 — no existe para leer; nada que simplificar en un archivo ausente. Quedan `bin/sdd`, `.claude/rules/{conventions,domains,git}.md` y `.specify/templates/rules/{conventions,domains,git,model-overrides,testing}.md` tras la letra literal de la lista. Estos ocho últimos son prosa de convenciones/templates-semilla (leídos con `.claude/rules/*.md` y `.specify/templates/rules/*.md`, confirmado por lectura: bullets y placeholders, sin nesting ni lógica) — tratados como fuera de alcance por el mismo razonamiento "prosa, no código" que ya excluye `.claude/CLAUDE.md`, extendiendo el hueco que 023 ya había señalado dos veces en este mismo archivo (`research/**/*.md`, registros `.pr-opened`) sin agregarlos a la lista — eso sigue siendo un fix aparte, no de esta pasada.
- **`bin/sdd`** (único archivo de código en scope): diff completo revisado contra `cba3f9a` — 287 líneas, +16/-271, pura eliminación (`cmd_open_pr`, `build_pr_title`, `build_pr_body_file`, `append_decisions_capped`, `write_pr_opened_sentinel`, `PR_BODY_MAX_CHARS`, la rama `ready-to-pr` de `detect_feature_phase`, el arm de dispatch, la entrada de `usage()`). `grep` confirma cero referencias sobrevivientes a los diez símbolos retirados. El único código *nuevo* (no eliminación) es `cmd_domain_vocab`'s `section="$(cat "$file")"` reemplazando la llamada a `extract_section` — exactamente el bloque que el prompt de esta pasada marcó como "trivially short — that is the point, not an opportunity". No se tocó: el resto del cuerpo (loop awk de comment-stripping, filtro, exit codes) es byte-idéntico al ya revisado en pasadas de 023. No se aplicó ningún cambio.
- **Baseline**: pass (97/97, 2 suites) | **Post-edit**: pass (97/97, 2 suites, no code changed)
- **Commit**: none — `SCOPED_FILES` no vacío en el filtro léxico, pero cero archivos terminaron con una modificación real tras la revisión (`git status --short` limpio antes y después). Llamar `sdd commit-slice` sin nada en staging habría devuelto el mismo `error: nothing staged for commit` que el propio harness de tests reproduce como caso esperado — se trata como equivalente al camino "SCOPED_FILES vacío" del protocolo (nada que simplificar, nada que commitear), no como una falla.

## JUDGMENT-DAY-HIGH — 024-remove-auto-pr

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | El mecanismo `sdd-sweep-exempt:start`/`:end` —lo único entre una referencia colgada real y un AC5 verde— está **enforced solo por prosa en un comentario**, sin ningún chequeo. Dos formas de derrotarlo, las dos reproducidas por el judge y por mí: **(a)** envolver una instrucción colgada real en los marcadores, en cualquier archivo del árbol barrido: metí *"corré `sdd open-pr <feature-id>`"* en `sdd-next/SKILL.md`, un archivo de orquestación vivo que un agente lee y obedece, y el barrido siguió verde. **(b)** Un marcador **desbalanceado** —un `:start` sin `:end`, o sea un typo o un merge mal resuelto— silencia todo el resto de ese archivo; lo probé en `sdd-auto/SKILL.md` con una instrucción colgada al final y pasó verde. El scanner trata el marcador como substring en cualquier línea de cualquier archivo barrido: sin restricción a `tests/sdd.test.js`, sin chequeo de balance, sin conteo fijado. La afirmación de `decisions.md` de que "cada bloque existe solo alrededor de una assertion de prueba-de-eliminación" es cierta hoy únicamente porque nadie la violó todavía. |
| 2 | medium | AC4 no tiene **ninguna** cobertura automatizada: ni los dos strings de comando, ni la llamada a `sdd base-branch $ARGUMENTS`, ni la rama de "imprimir sin resolver". El `plan.md` lo etiqueta "Manual", así que es hueco consciente — pero es la única prosa cuyo contenido *es* el criterio de aceptación, ejecutada por el modelo más barato del pipeline, sin nada que detecte deriva si una edición futura borra el Step 3.6 o cambia los comandos. |
| 3 | medium | El `walk()` del barrido usa `Dirent.isDirectory()`/`isFile()`, que **no siguen symlinks**: un archivo o directorio symlinkeado bajo cualquier raíz barrida es invisible. Reproducido. No es hipotético para esta herramienta: `link_or_copy()` de `bin/sdd` usa `ln -sfn` por defecto, y así es como despliega `.claude/skills/*` y `.claude/rules/*.md` en los proyectos consumidores. Hoy no hay symlinks bajo las raíces barridas de este repo. |
| 4 | low | `domain-vocab` con `domains.md` ilegible crashea con error crudo y exit 1, no el exit 3 silencioso que documenta el spec. **Pre-existente**, verificado idéntico contra la implementación anterior en `cba3f9a` — ni introducido ni empeorado por este feature. |

**Respuesta del judge a la meta-pregunta, y es la más interesante de las cinco veces que se hizo**: el barrido **sí** funcionó como red mecánica. Agarró cuatro hits reales no anticipados durante T005, y alcanza dotfiles, archivos sin extensión y directorios anidados (verificado). Es una mejora estructural genuina sobre 022 y 023, donde el fallo era un harness no representativo: acá se sidestepea esa clase borrando el código en vez de parchearlo, y usando un recorrido exhaustivo del filesystem en vez de enumerar call sites a mano.

Donde falla es en **una de sus dos escotillas**, y no son simétricas. La exclusión por directorio es sólida — el judge buscó los diez literales *y* paráfrasis no literales ("open a draft PR through the CLI") en `.claude/**` y `.specify/templates/**` y no encontró nada colgado. La exención por marcadores no: es exactamente la misma forma de fallo que describen los postmortems de 022 y 023 — un mecanismo que se ve completo contra los casos que su autor tenía en mente (los cinco spans conocidos) y que nunca se verificó contra la forma de un error real. **Quinto dato, resultado mixto: la estrategia del barrido es un paso adelante real, pero su propia excepción se shipeó con la misma forma de supuesto no verificado que motivó construirla.**

Source: sdd-judge, review-feature phase
Date: 2026-08-31

### Corroboración del reviewer (misma ronda, verificación independiente)

Reviewer: FAIL, reproduciendo los dos ataques con mutaciones distintas a las del judge. Tres aportes propios:

1. **El árbol está limpio HOY**: caminó el repo a mano buscando los diez símbolos y encontró 28 hits, **todos** adentro de los cinco spans documentados. Cero huérfanos, cero zona exenta sin documentar. Los cinco spans son genuinamente pruebas-de-eliminación — el mecanismo es honesto hoy; lo débil es lo que impide que deje de serlo.
2. **Suite verde por slice verificado de verdad**, con `git worktree` en los cinco commits: 163 → 134 → 96 → 96 → 97. La estrategia de rebanado funcionó.
3. **AC4 sin cobertura automatizada** (UNTESTED): ni los dos strings de comando, ni la llamada a `sdd base-branch`, ni la rama de fallo. `plan.md` lo declara "Manual", así que es hueco consciente — pero es la única AC cuyo contenido *es* prosa, ejecutada por el modelo más barato del pipeline.
4. MINOR, y es del orquestador: la entrada de `decisions.md` que dice "spec.md quedó en 690 palabras" ya eran 727 en ese commit. El log derivando de lo que describe, en el repo cuya doctrina es exactamente esa.

[2026-08-31] JUDGMENT-DAY-HIGH RESOLUTION: el usuario eligió **endurecer el barrido y cubrir AC4**. Alcance: (a) los marcadores de exención se reconocen solo en `tests/sdd.test.js` — en cualquier otro archivo del árbol barrido, encontrar el marcador cuenta como HIT en vez de crear zona exenta; (b) validar que cada `:start` tenga su `:end` antes del siguiente `:start` o del EOF, fallando fuerte si no; (c) dos tests de regresión que fijen los dos agujeros reproducidos; (d) `walk()` con `statSync` para seguir symlinks, cerrando el hallazgo medium — relevante porque `link_or_copy()` de `bin/sdd` despliega con `ln -sfn` por defecto; (e) un test que fije el contenido del Step 3.6 de `sdd-archive-feature.md`.

Razón: el feature entero se apoya en el barrido como prueba mecánica de que no quedó nada colgado. Shipearlo con un agujero por el que pasaron tres revisores seguidos lo convertiría en teatro.

## Review fix cycle — 2026-08-31

Implementadas las cinco líneas de la resolución de arriba, todo en `tests/`, sin tocar `bin/sdd`:

- **(a)+(b) `findHits()` reescrita** en `tests/sweep-retired-symbols.test.js`: los marcadores `sdd-sweep-exempt:start`/`:end` solo tienen significado en `tests/sdd.test.js` (nueva constante `MARKER_EXEMPT_FILE`) — en cualquier otro archivo del árbol barrido, encontrar el marcador como substring de línea es un HIT, no abre zona exenta. Dentro de `tests/sdd.test.js`, cada `:start` debe cerrar con `:end` antes del próximo `:start` o del EOF; desbalanceado lanza `Error` nombrando archivo y línea (revisado con `.toThrow(/unbalanced/i)`).
- **(c) Dos tests de regresión** en el mismo archivo, contra fixtures reales en disco (`fs.mkdtempSync` + `fs.writeFileSync`/`readFileSync`, no strings inline): uno reproduce el ataque (a) — una `SKILL.md` de fixture con `sdd open-pr <feature-id>` envuelto en los marcadores — y confirma que produce hits (el símbolo Y el marcador mismo); el otro reproduce el ataque (b) — un `:start` sin `:end` pasado como si fuera `tests/sdd.test.js` — y confirma que `findHits()` lanza. Ambos confirmados en RED contra la implementación previa antes de aplicar el fix (ver Validations-Output del envelope).
- **(d) `walk()` reescrita** con `fs.statSync` (sigue symlinks) en vez de `Dirent.isDirectory()`/`isFile()` (no los sigue), más guarda de ciclo vía `Set` de `realpathSync` de directorios visitados. Verificado a mano con un directorio symlinkeado real: el archivo detrás del symlink aparece en el walk.
- **(e) Test de pinning para el Step 3.6** en `tests/sdd.test.js` (`archive-feature's Step 3.6 resolves the base branch...`, 024 AC4): fija los dos comandos literales, la llamada a `sdd base-branch $ARGUMENTS`, y ambas ramas (éxito sustituye `<base>`, fallo lo deja sin resolver). No se tocó `sdd-archive-feature.md` — Step 3.6 ya existía de T004; esto solo lo cubre. El test además re-verifica `archiveStep35Line()` y el conteo de 2 fences del test T007 para confirmar que no se movieron.
- **Mutación de control repetida**: reintroducir `cmd_open_pr` en `bin/sdd` (revertido después) sigue siendo detectado por el barrido real — el endurecimiento no debilitó la detección de referencias colgantes genuinas.
- **Corrección aparte**: la entrada de más arriba que decía "690 palabras" corregida a 747 (conteo real verificado con `wc -w` en este commit).

Suite completa: 97 → 100 tests (2 suites), verde antes y después, con Node en el PATH y sin él (`bin/sdd status` corrido con PATH filtrado). Nada fuera de `tests/` y este `decisions.md` cambió.

## Simplify: 2026-08-31 — /simplify-code (segunda pasada)
- **Files simplified**: none
- **Fuente canónica usada**: `.claude/agents/sdd-simplify-code.md` de este repo (no la copia genérica del prompt de lanzamiento) — ya incluye `.claude/agents/**/*.md` y `docs/adr/**/*.md` en el filtro de exclusión SDD, y el paso 5.5 de commit. Confirmado como el gobernante real: `bin/sdd update` lo sincroniza por byte-diff, es el archivo que Claude Code carga para este agente.
- **Alcance recalculado** (`git diff --name-only cba3f9a..HEAD`, 24 archivos, sidecar `.parent-branch` verificado): tras filtros de tests/SDD-artifacts quedan literalmente en scope `.claude/rules/{conventions,domains,git}.md`, `.specify/templates/rules/{conventions,domains,git}.md` y `bin/sdd`. Ninguno cambió desde la pasada anterior — `git log` confirma que sus últimos commits son T001-T003, todos anteriores a la pasada de simplify previa que ya los revisó exhaustivamente (bin/sdd: diff completo línea por línea contra `cba3f9a`, cero cambios; rules/templates: tratados como prosa fuera de alcance, mismo razonamiento que specs/SKILL.md). `src/extract-section.js` no existe (borrado por T003) — nada que leer. Re-review de contenido sin cambios no aporta nada nuevo; se mantiene la conclusión ya documentada arriba.
- **El único commit nuevo desde la pasada anterior** (`54819c8`) toca solo `decisions.md`, `tests/sdd.test.js` y `tests/sweep-retired-symbols.test.js` — los tres excluidos por el filtro de tests (o son este mismo archivo de decisiones). Leí `tests/sweep-retired-symbols.test.js` completo (294 líneas: comentario de cabecera, `findHits()` con las dos ramas — archivo exento vs. no exento —, `walk()` con symlinks, y los dos tests de regresión JUDGMENT-DAY-HIGH #1). Se lee con claridad: nombres explícitos, un comentario por invariante no obvio, y las dos ramas de `findHits()` no se pueden fusionar sin perder la asimetría que es el punto del fix (fuera de `tests/sdd.test.js` el marcador ES un hit; adentro, abre/cierra zona exenta con balance-check). Ninguna simplificación identificada — comprimirlo sería la regresión que la instrucción de lanzamiento advertía, no una mejora.
- **Nota sobre el brief de lanzamiento**: pedía tratar ese archivo como "el único candidato real" y sopesar simplificarlo. El filtro de tests (`**/tests/**`) y la NEVER list ("never touch test files — scope already excludes them") de este mismo agente lo excluyen de forma no negociable, con o sin ese encuadre. Se leyó y se evaluó como pidió el brief; no se editó, porque el propio mecanismo que estoy ejecutando lo prohíbe. Sin conflicto real en la práctica: la conclusión de lectura ("queda claro, no tocar") coincide con lo que el filtro ya forzaba.
- **Baseline**: pass (100/100, 2 suites) | **Post-edit**: pass (100/100, 2 suites, no code changed — sin ediciones, working tree limpio antes y después)
- **Commit**: none — `SCOPED_FILES` (tras aplicar precedente sobre contenido sin cambios) vacío; sin nada que commitear, `sdd commit-slice` no se invocó (paso 5.5 se saltea explícitamente cuando `SCOPED_FILES` está vacío).

## JUDGMENT-DAY-HIGH (2) — 024-remove-auto-pr (re-review ciclo 1)

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | El arreglo del ciclo anterior angostó la **ubicación** de la escotilla (marcadores solo válidos en `tests/sdd.test.js`) y agregó chequeo de **balance**, pero **nunca verifica el contenido** que deja pasar adentro de ese archivo. Reproducido por el judge y por mí: un TODO de mantenimiento real —*"cuando aterrice el gate nuevo, correr `sdd open-pr <feature-id>` a mano acá"*— envuelto en marcadores balanceados y legalmente ubicados, pasa **verde**. Mismo fallo silencioso que el ataque original, reubicado de "cualquier archivo barrido" al único archivo que el arreglo enseñó a confiar. |
| 2 | low | El test de fijación de AC4 usa `.toContain()` independientes sin restricción de orden: invertir la prosa para que `gh pr create` vaya antes que `git push` —operacionalmente al revés, no se puede abrir PR de una rama sin pushear— pasa igual. |
| 3 | low | El seguimiento de symlinks no tiene containment al repo: un symlink bajo una raíz barrida que apunte a `docs/`/`specs/` (excluidos a propósito) o afuera del repo se recorre igual. Reproducido: 24 falsos positivos contra prosa histórica legítima de los ADRs, y filtrado de contenido de `/tmp` a la salida del test. Falla ruidoso, no en silencio, así que es blast radius y no un agujero en AC5. El guard de ciclos sí es sólido (self-loop y ciclo de dos nodos, los dos limpios). |
| 4 | low | Una línea con `:start` y `:end` juntos se malinterpreta: la rama de `:start` corta antes de ver el `:end` de la misma línea, deja el span abierto y después culpa a un marcador posterior en el mensaje de error. Falla ruidoso, pero el diagnóstico miente. |

**Respuesta del judge a la meta-pregunta, sexta vez y la más incómoda**: *"Angostar la **ubicación** de una escotilla no es la misma operación que verificar su **contenido**, y solo se hizo la primera."* Cerrar el agujero **no terminó la regresión: la movió una capa más adentro**. Los dos ataques nombrados están cerrados de verdad y pinneados con fixtures reales, no con reimplementaciones de la lógica bajo test. Pero lo único que separa esto de un incidente es la misma promesa en prosa que ya falló antes —un comentario, no un chequeo— más que nadie lo haya hecho todavía. Es literalmente la frase con que se describió el estado *previo* al arreglo.

Los hallazgos #2 y #3 del ciclo anterior (cobertura de AC4, symlinks invisibles) quedaron **genuinamente arreglados**. El #4 (crash con `domains.md` ilegible) sigue en pie, confirmado pre-existente e idéntico a `cba3f9a`, y el judge explícitamente no lo escala: 024 es un feature de remoción de símbolos, no de manejo de errores de `domain-vocab`.

Verdict del judge: FAIL.
Source: sdd-judge, review-feature phase (re-review ciclo 1)
Date: 2026-08-31

[2026-08-31] JUDGMENT-DAY-HIGH (2) RESOLUTION: el usuario eligió **las dos cosas** — conteo fijado y chequeo estructural, más los tres low.

Vale registrar por qué las dos y no una: el **chequeo estructural** verifica el contenido, que es la operación que faltaba y la que el judge nombró. El **conteo fijado** hace otra cosa distinta en especie — no agrega una regla que se pueda satisfacer con una construcción conforme pero incorrecta, sino que vuelve la escotilla **contable**: agregar un span obliga a editar un número, y eso aparece en el diff de cualquier review. El primero cierra el agujero conocido; el segundo hace que el próximo, sea cual sea, no pueda entrar en silencio. Cada arreglo anterior de esta cadena agregó reglas; ninguno había hecho auditable la excepción.

## Review fix cycle 2 — 2026-08-31

Implementadas las cinco líneas de la resolución de JUDGMENT-DAY-HIGH (2), todo en `tests/`, sin tocar `bin/sdd`:

- **Chequeo estructural de contenido** (`isGenuineProofOfRemoval()`, nueva, en `tests/sweep-retired-symbols.test.js`): un span exento solo cuenta como exención si, en cualquier parte de sus líneas (no necesariamente la misma línea), hay **a la vez** una llamada `expect(...)` real y una referencia a un símbolo retirado en **código**, no en un comentario `//` (helper `codePortion()` recorta el comentario antes de buscar). Verificado contra los cinco spans reales de `tests/sdd.test.js` sin reescribir ninguno: cuatro tienen el símbolo retirado dentro del propio `expect(...)`; el quinto (el test de AC6 del sentinel `.pr-opened` colgado) lo pasa como argumento literal a `fs.writeFileSync` junto a un `expect` que valida el efecto, sin nombrar el símbolo dentro del `expect` mismo — el chequeo acepta ambas formas. Documentado en el propio código el límite conocido: una constante de código muerta y sin usar que solo *contenga* el texto de un símbolo retirado, junto a un `expect` no relacionado, técnicamente pasaría — el segundo mecanismo (conteo fijado) es la respuesta a ese residuo, no el chequeo de contenido por sí solo.
- **Conteo fijado** (`EXPECTED_EXEMPT_SPAN_COUNT = 5`, comentario explicando por qué no es redundante con el chequeo estructural): un test nuevo cuenta los `:start`/`:end` literales en `tests/sdd.test.js` y falla si no son exactamente 5.
- **Marcador en una sola línea**: `findHits()` ahora detecta `:start` y `:end` en la misma línea *antes* de evaluar las ramas individuales, y la trata como su propio span abierto-y-cerrado en el acto — antes, la rama de `:start` cortaba sin ver el `:end` de esa misma línea, dejaba el span abierto, y el *próximo* `:start` real del archivo tiraba "unbalanced" culpando a un marcador ajeno. Fijado y confirmado con fixture: ya no lanza, y el hit (si el contenido de esa línea no es una prueba genuina) se reporta en su propia línea.
- **Containment de symlinks** (`walk()` gana un cuarto parámetro opcional `containmentRoot`): un symlink cuyo destino resuelto cae afuera del repo, o adentro de `docs/`/`specs/`, ya no se seguía — se saltea. Parámetro opcional a propósito: los dos fixtures de JUDGMENT-DAY-HIGH #1 caminan un directorio temporal ajeno al repo y no deben verse afectados; sin `containmentRoot` el comportamiento es idéntico al anterior. `livingSurfaceFiles()` pasa el real path del repo. Un tercer test confirma que esto no es sobre-restrictivo: un symlink que se queda adentro del repo y fuera de `docs`/`specs` se sigue caminando igual.
- **Orden de AC4** (`tests/sdd.test.js`, no el archivo de sweep): el test de fijación del Step 3.6 ahora compara `indexOf()` de los dos comandos además de los `toContain()` independientes que ya tenía — invertir la prosa para que `gh pr create` aparezca antes que `git push` ahora falla (antes pasaba igual, operacionalmente al revés). Verificado a mano con una prueba de `indexOf` que confirma la aserción detecta el orden invertido.
- **Siete tests nuevos**, todos en `tests/sweep-retired-symbols.test.js` (en `tests/sdd.test.js` se modificó un test existente, no se agregó ninguno): dangling-comment-attack, span-count pin, same-line-marker, dos de containment de symlink (por fuera del repo / adentro de docs-specs), uno de no-sobre-restricción del containment, y uno que confirma los cinco spans reales siguen pasando el chequeo de contenido.
- **RED confirmado antes del fix**: se armó una versión temporal (implementación original de `cba3f9a`/ciclo 1 + los siete tests nuevos) y se corrió sola — 6 de 10 tests de ese archivo fallaron, incluyendo el `ReferenceError` esperado de los símbolos que todavía no existían y, más interesante, el test del marcador en una sola línea reprodujo el bug exacto de la descripción: tiró `"tests/sdd.test.js:3: unbalanced ... opened at line 1"`, culpando a la línea 3 (un marcador ajeno) del problema real de la línea 1. Ver Validations-Output del envelope para la salida real.
- **Autodefensa pedida por el brief**: qué forma de mal uso seguiría pasando el fix — una constante de código sin usar cuyo único contenido es el texto de un símbolo retirado, ubicada junto a un `expect(...)` no relacionado dentro del mismo span, satisface `isGenuineProofOfRemoval()` sin ser una prueba genuina. Documentado en el comentario de la función. Lo que acota ese residuo es el conteo fijado: cualquier span nuevo, disfrazado o no, exige tocar `EXPECTED_EXEMPT_SPAN_COUNT`, lo cual aparece en el diff que cualquier reviewer lee.

Suite completa: 100 → 107 tests (2 suites), verde antes y después. Nada fuera de `tests/` y este `decisions.md` cambió — `bin/sdd` intacto, los cinco spans legítimos intactos, las exclusiones de directorio (`docs/`, `specs/`) intactas.

## Simplify: 2026-08-31 — /simplify-code (tercera pasada)
- **Files simplified**: none
- **Alcance recalculado** (`git diff --name-only cba3f9a..HEAD` vía `.parent-branch` sidecar, sin caer a `main`): 25 archivos brutos; tras el filtro de tests y el filtro extendido de artefactos SDD de `.claude/agents/sdd-simplify-code.md` (que ya incluye `.claude/agents/**/*.md` y `docs/adr/**/*.md`), quedan literalmente en `SCOPED_FILES`: `.claude/rules/{conventions,domains,git}.md`, `.specify/templates/rules/{conventions,domains,git}.md` y `bin/sdd`.
- **Confirmado sin cambios desde la segunda pasada**: `git diff --name-only 54819c8..b071cb3` devuelve solo `decisions.md`, `tests/sdd.test.js` y `tests/sweep-retired-symbols.test.js` — los siete archivos de `SCOPED_FILES` son byte-idénticos a los ya revisados exhaustivamente en las dos pasadas anteriores (bin/sdd línea por línea contra `cba3f9a`; rules/templates como prosa de convenciones fuera de alcance por el mismo razonamiento que excluye `.claude/CLAUDE.md`). Se carga ese juicio hacia adelante sin releer, tal como pidió el brief de esta pasada — no hay nada nuevo que analizar en contenido sin cambios.
- **`tests/sweep-retired-symbols.test.js` leído completo** (591 líneas, creció desde las 294 de la segunda pasada por el review fix cycle 2: `isGenuineProofOfRemoval()`, `EXPECTED_EXEMPT_SPAN_COUNT`, manejo de marcador en una sola línea, containment de symlinks). Es un archivo de test — excluido por el filtro y por la NEVER list ("never touch test files") — pero el brief de lanzamiento pidió confirmar que lee con claridad en vez de asumirlo. Confirmado: nombres explícitos (`isGenuineProofOfRemoval`, `codePortion`, `EXPECTED_EXEMPT_SPAN_COUNT`), un comentario por invariante no obvio, y cada mecanismo nuevo trazado a su hallazgo de JUDGMENT-DAY-HIGH (2) con el razonamiento de por qué no es redundante con el anterior. Verificado a mano el punto central del brief: el chequeo de contenido y el conteo fijado cubren fallos distintos — un sexto span *genuino* (`expect()` real + símbolo real) seguiría rompiendo el test de conteo (`EXPECTED_EXEMPT_SPAN_COUNT = 5`), que es exactamente la propiedad que el comentario del archivo reclama y que haría de fusionarlos una regresión, no una mejora.
- **Única observación, no accionable**: el loop `for (const symbol of RETIRED_SYMBOLS) { if (line.includes(symbol)) hits.push(...) }` aparece dos veces en `findHits()` (rama no-exenta y rama exenta fuera de un span), idéntico en ambas. Es compresible, no incorrecto — y de todas formas está fuera de alcance por ser un archivo de test. No se reporta como hallazgo porque no es un bug, solo se deja anotado por si una edición futura de ese archivo (fuera de esta pasada) quiere unificarlo.
- **Nada genuinamente incorrecto encontrado** en el archivo de test ni en el resto del scope. No se hizo ninguna edición.
- **Baseline**: pass (107/107, 2 suites) | **Post-edit**: pass (107/107, 2 suites, sin cambios de código — working tree limpio antes y después)
- **Commit**: none — cero archivos terminaron con una modificación real; `git status --short` limpio antes y después, nada que stagear.

[2026-08-31] CORRECCIÓN (reviewer, re-review ciclo 2): el registro afirmaba que un symlink que escapa del repo solo puede agregar falsos positivos y nunca esconder una referencia colgada viva. Es demasiado amplio: un symlink hacia contenido legítimamente **vivo** fuera del repo queda sin escanear — el modo de falla opuesto, reproducido contra el `walk()` real. Hoy inalcanzable en este repo porque `bin/sdd` rechaza symlinks auto-referenciales cuando el target es SDD_HOME; dejaría de serlo si este repo se manejara como consumidor de un SDD_HOME separado. Corrección de documentación, no defecto.

## JUDGMENT-DAY-HIGH (3) — 024-remove-auto-pr (re-review ciclo 2)

Reviewer: PASS WITH WARNINGS. Judge: FAIL.

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | `codePortion()` saca comentarios de línea (`//`) antes de buscar referencias a símbolos retirados, para que una nota en prosa no cuente como "el código usa este símbolo". **No conoce `/* */`.** Un comentario de bloque vuelve entero de `codePortion()` y se lee como referencia a nivel de código — una de las dos condiciones que exige `isGenuineProofOfRemoval()`. Combinado con cualquier `expect()` real ya presente en el span (los cinco reales tienen uno), el span pasa como prueba genuina. **Y el golpe es al segundo mecanismo**: inyectado adentro de un span YA EXISTENTE en vez de agregar uno nuevo, el conteo sigue en 5/5. Reproducido por el judge contra las funciones reales y por mí contra el archivo real: marcadores 5/5, los diez tests del barrido verdes, con un `sdd open-pr` colgado adentro del archivo confiable. |
| 2 | medium | El containment de symlinks excluye también el caso legítimo que el comentario del propio archivo llama "no hipotético": los `.claude/skills/*` de un proyecto consumidor **son** symlinks hacia SDD_HOME. Inerte hoy — este repo ES SDD_HOME y sus skills son archivos reales, y el test del barrido no se distribuye vía `.specify/templates`. |

**Respuesta del judge a la meta-pregunta, séptima vez**: la regresión **no terminó**, y esta vez derrota los dos mecanismos a la vez. La garantía del conteo —"cualquier cambio en la cantidad de spans cuesta un diff visible"— es cierta solo para el **alta** de un span; no dice nada sobre **editar el texto adentro** de uno de los cinco que ya existen y ya pasan. Es un modo de falla materialmente distinto: los ciclos 1 y 2 cerraron formas de plantar una excepción *nueva*; esto derrota el mecanismo mutando una *preexistente y confiable*, que ninguno de los dos guardianes fue construido para notar.

El judge dejó constancia de que buscó razones para decir que terminaba: reconoce que regla-versus-conteo es una diferencia categórica real, y verificó que el conteo hace exactamente lo que promete para el caso que promete. Simplemente no cubre el caso que importó.

[2026-08-31] JUDGMENT-DAY-HIGH (3) RESOLUTION: el usuario eligió **sacar la escotilla en vez de custodiarla**. Los cinco tests de prueba-de-eliminación se mudan a su propio archivo, excluido por ruta — el mismo mecanismo que ya se usa para el archivo del barrido. Desaparecen los marcadores, `isGenuineProofOfRemoval()`, `EXPECTED_EXEMPT_SPAN_COUNT` y la necesidad del hash por span que sugería el judge.

Razón, y es la lección acumulada de la sesión: cuatro rondas endureciendo un guardián que ya tiene 591 líneas, para custodiar un borrado que el reviewer verificó completo a mano en una pasada. Cada ronda cerró el ataque anterior y dejó el siguiente una capa adentro. Existe un arreglo que **sí** termina la regresión —un hash por span, que hace visible cualquier edición y no solo las altas— pero agrega otra capa. Mudar los tests elimina la categoría entera: sin escotilla no hay nada que custodiar, y la exclusión por ruta se audita de un vistazo. Es el mismo movimiento que `domains.md` sobre el parser de Markdown: no parsear mejor, sacar la necesidad de parsear.

## Review fix cycle 3 — 2026-08-31

Implementada la resolución de JUDGMENT-DAY-HIGH (3) tal como se decidió: sacar la escotilla, no agregarle una quinta capa.

- **Los cinco tests de prueba-de-eliminación se leyeron completos antes de mover nada** (AC1 unknown-command, la política de `git.md` de T013, las dos de wiring de AC4 —`sdd-next`/`sdd-auto` y `CLAUDE.md`—, y el sentinel colgado de AC6): los cinco son pruebas de AC legítimas, ninguno es un guardián disfrazado de la escotilla. Los cinco nombran el símbolo retirado en algún punto de su cuerpo (no solo en un comentario), así que ninguno calificaba para quedarse en `tests/sdd.test.js` por no necesitarlo — la salida "decir que se queda" del brief de lanzamiento no aplicó a ninguno.
- **Mudados literalmente** a `tests/retired-symbol-proofs.test.js` (nuevo archivo): el test de AC6 se llevó consigo su helper local `archiveFeature()` (usado solo por ese test, dentro de su mismo `describe`) para no dejarlo huérfano en `sdd.test.js`. Los otros cuatro no comparten fixtures con tests que se quedan. `makeTempProject()` y `sddFail()` se duplicaron en el archivo nuevo (mismo patrón que ya usa `tests/sweep-retired-symbols.test.js` para sus propios `repoRoot`/`fs`/`path`) en vez de importarlos — un `require()` de `sdd.test.js` volvería a ejecutar los 92 tests que se quedaron ahí.
- **`tests/sweep-retired-symbols.test.js`**: eliminados `EXEMPT_START_MARKER`, `EXEMPT_END_MARKER`, `MARKER_EXEMPT_FILE`, `codePortion()`, `isGenuineProofOfRemoval()`, `EXPECTED_EXEMPT_SPAN_COUNT` y las dos ramas de `findHits()` que los usaban — queda una sola rama, sin casos especiales. `SELF_PATH` (un string) se volvió `EXCLUDED_PATHS` (un `Set` de dos strings): el mismo archivo del barrido, y el archivo nuevo. Los seis tests que existían solo para custodiar el mecanismo de marcadores (JUDGMENT-DAY-HIGH #1 completo, JUDGMENT-DAY-HIGH #2 completo) se borraron con él — no tenían nada que seguir probando una vez que el mecanismo que probaban dejó de existir. El bloque de comentario de cabecera se reescribió para describir el diseño actual, no el historial de rondas que ya no aplica (ese historial queda en este mismo `decisions.md`).
- **`tests/sdd.test.js`**: los cinco pares de marcadores desaparecieron junto con los tests que envolvían. En su lugar quedó un comentario corto por hueco, señalando adónde se mudó cada test y por qué — **redactado a propósito para no repetir los strings retirados de forma literal** (ej. "el comando de creación de PR retirado" en vez de nombrarlo con el guión), porque este archivo ahora está barrido sin ninguna excepción y un comentario explicativo escrito descuidadamente rompía el barrido contra sí mismo en la primera corrida (ver el RED de abajo).
- **Confirmado que el hueco no se ensanchó**: `tests/retired-symbol-proofs.test.js` tiene exactamente esos cinco tests y nada más — sin tests adicionales, sin lógica, solo los dos helpers que necesitan y el helper local de AC6. Verificado leyendo el archivo completo tras terminar la mudanza.
- **RED real durante el trabajo, no hipotético**: la primera corrida completa post-mudanza falló — mis propios comentarios de reemplazo en `tests/sdd.test.js` citaban los títulos originales de los tests entre comillas (`"open-pr no longer exists"`, etc.), y esas citas contienen los símbolos retirados como substring literal. El barrido los marcó como 8 hits en `tests/sdd.test.js`. Reescritos en prosa que describe el comando/símbolo sin citarlo literalmente; la corrida siguiente pasó limpia. Es la prueba más directa de que el diseño funciona como se pretende: cero excepciones implícitas, ni siquiera para un comentario bienintencionado.
- **Tres mutaciones de control**, todas revertidas después de confirmar:
  1. Reintroducir `cmd_open_pr` en `bin/sdd` → detectado (`bin/sdd:1337: "cmd_open_pr"`).
  2. Agregar una instrucción colgada (`` `sdd open-pr <feature-id>` ``) a la prosa de `.claude/agents/sdd-archive-feature.md` → detectado (`.claude/agents/sdd-archive-feature.md:112: "open-pr"`).
  3. Agregar la misma instrucción colgada directamente a `tests/sdd.test.js` → **detectado** (`tests/sdd.test.js:2348: "open-pr"`). Este es el caso que antes se podía esconder envolviéndolo en un span balanceado con código real cerca (JUDGMENT-DAY-HIGH #2/#3); sin mecanismo de excepción en ese archivo, ya no hay dónde esconderlo.
- **Los cinco tests mudados corren y pasan** en su archivo nuevo, de forma aislada y como parte de la suite completa, con las mismas aserciones que tenían en `sdd.test.js` — no se reescribió ninguna aserción, solo la ubicación.

Aritmética de la suite: **107 → 101 tests** (2 → 3 suites). Se explica en dos movimientos independientes: mudar los cinco tests de prueba-de-eliminación es neto cero (salen de `sdd.test.js`, entran a `retired-symbol-proofs.test.js`: 97→92 y 0→5); borrar los seis tests que custodiaban el mecanismo de marcadores en `tests/sweep-retired-symbols.test.js` es neto −6 (10→4, sin reemplazo — no hay nada que reemplace un test que probaba una función que ya no existe). 92 + 4 + 5 = 101.

No se tocó `bin/sdd` — intacto desde T003, sin ninguna referencia a los diez símbolos retirados salvo dentro de las mutaciones de control, las tres revertidas. Tampoco se tocaron las exclusiones de directorio (`docs/`, `specs/`), el containment de symlinks, ni el test de orden de AC4 (`indexOf`) — se mudó intacto junto con el test de AC4 al que pertenece.

## Simplify: 2026-08-31 — /simplify-code (cuarta pasada)
- **Files simplified**: none
- **Alcance recalculado** (`sdd base-branch 024-remove-auto-pr` vía el sidecar `.parent-branch` → `cba3f9a`, `git diff --name-only cba3f9a..HEAD`): 25 archivos brutos. Tras el filtro de tests y el filtro extendido de artefactos SDD de `.claude/agents/sdd-simplify-code.md` (que ya incluye `.claude/agents/**/*.md` y `docs/adr/**/*.md`), `SCOPED_FILES` queda idéntico al de la tercera pasada: `.claude/rules/{conventions,domains,git}.md`, `.specify/templates/rules/{conventions,domains,git}.md` y `bin/sdd`. `src/extract-section.js` aparece en el diff crudo pero está borrado (`D`, por T003) — no existe para leer.
- **Confirmado byte-idéntico a las tres pasadas anteriores**: `git log --oneline -1 -- <archivo>` para cada uno de los siete apunta a T001 (`2a302aa`), T002 (`aaf746e`) o T003 (`5b96fd5`) — ninguno de los tres commits de review fix cycle (`54819c8`, `b071cb3`, `13b3202`) los toca; `git diff --name-only 54819c8..HEAD` devuelve solo `decisions.md` y los dos archivos de test. Se carga el juicio de las tres pasadas anteriores hacia adelante sin releer letra por letra, tal como pidió el brief de esta pasada: `bin/sdd` es diff de pura eliminación ya revisado línea por línea; las seis páginas de `rules`/`templates` son prosa de convenciones ya leída y sin nesting ni lógica.
- **`tests/sweep-retired-symbols.test.js` (235 líneas) y `tests/retired-symbol-proofs.test.js` (230 líneas, nuevo) leídos completos** — son los únicos archivos que cambiaron desde la tercera pasada. Ambos son archivos de test: excluidos por el filtro de alcance y por la NEVER list ("never touch test files"). Confirmado que leen con claridad: un comentario de cabecera por archivo que explica el diseño y por qué existe la exclusión por ruta, nombres explícitos (`isWithin`, `walk`, `livingSurfaceFiles`, `findHits`, `EXCLUDED_PATHS`, `makeTempProject`, `sddFail`), un comentario por invariante no obvio (symlink vía `statSync` en vez de `Dirent.isDirectory()`, "scans less noise, not less coverage"). `tests/retired-symbol-proofs.test.js` contiene exactamente los cinco tests movidos (AC1 unknown-command, la política de `git.md`, las dos de wiring de AC4, el sentinel colgado de AC6) y nada más — confirma la afirmación de la pasada anterior de que el hueco no se ensanchó.
- **Nada genuinamente incorrecto encontrado.** No se hizo ninguna edición.
- **Baseline**: pass (101/101, 3 suites) | **Post-edit**: pass (101/101, 3 suites, sin cambios de código — working tree limpio antes y después)
- **Commit**: none — cero archivos terminaron con una modificación real; `git status --short` limpio antes y después, nada que stagear (equivalente al camino "SCOPED_FILES vacío" del protocolo, mismo tratamiento que la tercera pasada).

## REVIEW-CONSOLIDATION (re-review ciclo 3) — PASS WITH WARNINGS

Reviewer: **PASS**. Judge: **PASS WITH WARNINGS** (un medium). Los dos atacaron el diseño nuevo por separado y los dos concluyen que la regresión termina.

**Respuesta del judge a la meta-pregunta, octava y última vez — y esta vez es limpia**: los tres JUDGMENT-DAY-HIGH previos fueron bugs en la **lógica de un validador de contenido** (ubicación del marcador, balance, contenido del span, conteo de spans). Cada arreglo cerró el bug demostrado y dejó el resto de la lógica para volver a sondear. Borrar el mecanismo es un movimiento de otra clase: **ya no hay parser en el que pueda haber un bug de parseo**. La exclusión por ruta no tiene reglas, ni estado, ni conteo — nada que una entrada fabricada pueda derrotar, porque no intenta distinguir contenido bueno de malo. *"Angostar quién puede usar una escotilla converge a custodiarla para siempre; sacarla elimina la categoría de ataque, no la instancia más reciente."*

Ninguno de los dos lo aceptó de palabra:
- El judge diffeó los cinco tests mudados byte a byte contra su forma en `b071cb3` (idénticos salvo indentación), reprodujo el ataque del ciclo 3 (agarrado), corrió tres mutaciones de control (las tres agarradas) y probó el modo de falla de la exclusión: renombrarla con un typo pone el suite en rojo con 32 hits, **no lo abre en silencio**.
- El reviewer replicó el ataque de comentario de bloque adentro de un span existente —el que derrotó los dos mecanismos a la vez— y ahora se agarra (`tests/sdd.test.js:126`). Después atacó el punto débil nuevo: un test colgado adentro del archivo excluido pasa en silencio, confirmando que el agujero declarado es real.

**La distinción del reviewer sobre por qué el agujero nuevo no es el viejo con otra forma**: el viejo era un mecanismo que *parecía* funcionar y engañó a tres rondas de review; el nuevo es un archivo de 230 líneas, de un solo propósito, cuyo encabezado dice qué va adentro. Violarlo es un desajuste de propósito, no una regla satisfecha con una construcción astuta.

**Medium del judge, aceptado y en alcance**: `EXCLUDED_PATHS` y los dos archivos que nombra no tienen pin automatizado — una tercera exclusión silenciosa, o contenido ajeno adentro del archivo excluido, dependen de que alguien lo vea en el diff. Es la misma clase de exposición "confiá en el revisor" que la historia de este feature muestra que los revisores ya fallaron dos veces.

[2026-08-31] Resolución: **tomar el pin antes de archivar**. Fijar que `EXCLUDED_PATHS` sea exactamente esas dos rutas y que `tests/retired-symbol-proofs.test.js` tenga exactamente los cinco tests con sus títulos. Razón para tomarlo pese al veredicto habilitante: convierte el último punto de confianza en un chequeo, y **no puede reabrir la regresión** — es una igualdad sobre un conjunto y un conteo de títulos, sin lógica de contenido que una construcción pueda satisfacer torcidamente. Es la diferencia entre este pin y las cuatro reglas que fallaron antes.

## Implementación: 2026-08-31 — pin de EXCLUDED_PATHS + títulos (cierra el medium de re-review ciclo 3)

Agregadas dos aserciones en un `describe` nuevo al final de `tests/sweep-retired-symbols.test.js`, sin tocar `bin/sdd` ni ningún archivo de producción:

- **`EXCLUDED_PATHS` es exactamente ese Set de dos rutas** — `expect(EXCLUDED_PATHS).toEqual(new Set([...]))` — una tercera exclusión silenciosa rompe esta línea sola.
- **`tests/retired-symbol-proofs.test.js` tiene exactamente esos cinco tests, fijados por título** — para cada uno de los cinco títulos esperados, `content.toContain(`test("${title}"`)`; y por separado, `(content.match(/\btest\(/g) || []).length` debe ser 5. Las dos mitades cubren fallos distintos a propósito: renombrar un título rompe la primera sin tocar la segunda; agregar un sexto test (real o colgado) rompe la segunda sin tocar la primera.

Sin lógica de parseo: ninguna de las dos aserciones distingue contenido bueno de malo, así que no hay nada en lo que un ataque de construcción pueda apoyarse — la razón por la que el usuario aceptó tomar el pin pese a que el veredicto ya habilitaba archivar.

**Tres mutaciones de control, todas revertidas después de confirmar el rojo real** (ver salidas abajo):
1. Agregar una tercera ruta a `EXCLUDED_PATHS` → rojo en la aserción del Set (`toEqual` muestra la ruta de más).
2. Agregar un sexto test a `tests/retired-symbol-proofs.test.js` → rojo en el conteo (`Expected: 5, Received: 6`).
3. Renombrar uno de los cinco títulos → rojo en el `toContain` de ese título, antes de llegar al conteo.

Lo ya verificado en rondas anteriores sigue vigente sin cambios: el ataque de comentario de bloque contra `tests/sdd.test.js` sigue detectado, una referencia colgada real en `bin/sdd` sigue detectada, una instrucción colgada en la prosa de un agente sigue detectada, y los cinco tests mudados corren con sus aserciones intactas.

**Aritmética de la suite**: 101 → 103 tests (3 suites sin cambio) — los dos tests nuevos del pin, netos, nada más se movió ni se borró.
