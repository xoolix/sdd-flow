# Decisions

## Delta: 2026-08-26 — Alcance ampliado (decisión del usuario)
- **ADDED**: sexto arreglo — detección de specs terminados sin archivar. Hoy `sdd status` sin argumentos y fuera de una rama feature devuelve `error: not on a feature branch`; no hay forma de preguntar qué features quedaron sin archivar sin correrlo uno por uno. El pipeline rutea a archive después del review, pero si alguien para antes, nada avisa: `002-evaluator-optimizer-pipeline` estuvo terminada y sin archivar tres meses.
  **Principio que lo motiva (declarado por el usuario)**: cada spec terminado se archiva. No es solo higiene del repo — un spec viejo en `specs/` es contexto falso que el modelo lee como trabajo activo.
  Fix propuesto: `sdd status` sin feature-id lista todas las carpetas de `specs/` con su fase.

[2026-08-26T10:35:50Z] CLEANUP: cuatro carpetas de mayo (001, 002, 009, 018) movidas con `git mv` a `specs/archive/2026-05-17-<id>/`, usando la fecha de su último commit real. `specs/` queda solo con 020 (dogfood [HITL] pendiente), 022 (en curso) y `archive/`.

[2026-08-26T11:34:57Z] DISCOVERY-ACCEPTED: F2 — guard `git ls-files --error-unmatch` antes del `git add` en `--moved-from`.
[2026-08-26T11:34:57Z] DISCOVERY-ACCEPTED: F3 — lista explícita de fases no reintentables en §F de `sdd-phase-common.md`, replicada en los dos orquestadores. Descartado hardcodear archive y descartado un campo de frontmatter.
[2026-08-26T11:34:57Z] DISCOVERY-ACCEPTED: F12 — séptimo AC para la detección de specs sin archivar, vía `sdd status` sin feature-id.

## Delta: 2026-08-26 — Discovery Checkpoint
- **ADDED**: guard `git ls-files --error-unmatch` en `--moved-from`. El spec prometía "never a silent no-op" pero la forma nunca-trackeado-y-presente-en-disco hacía algo peor: staging silencioso como archivo nuevo.
- **ADDED**: lista de fases no reintentables en §F. El spec asumía que "archive no se reintenta" era una redacción; §F no tiene el concepto y hay que agregarlo.
- **ADDED**: AC7 y su superficie (`sdd status` sin feature-id). El sexto arreglo entró por delta después de que el spec se escribiera con seis AC.

[2026-08-26T11:35:50Z] SPEC-BUDGET: spec.md quedó en 799 palabras contra el tope de 650, tras dos pasadas de recorte. Con seis arreglos y ocho criterios de aceptación, los AC solos son ~250 palabras y recortarlos sacaría verificación. Cuarto spec consecutivo por encima del tope (020: 665, 021: 661, 022: 799) — el patrón sugiere que el budget no está calibrado para features que tocan contratos, o que la tabla de contrato debería vivir en plan.md.

## Delta: 2026-08-26 — Plan/tasks coherence review
- **MODIFIED**: `--moved-from` stagea con `git add -- "$path"`, NO con `git add -A -- "$path"`. El plan propuso `-A`, pero `tests/sdd.test.js` tiene un assert de feature 020 —`expect(codeOnly).not.toMatch(/git add (-A|--all)\b/)` dentro de `cmd_commit_slice`, filtrando comentarios— que la implementación rompería. Y el `-A` es innecesario: desde git 2.0 `git add <path>` stagea remociones, verificado en repo temporal (`git add -- specs/001-real` sobre un path trackeado y borrado sale 0 y stagea `D`). El guard `git ls-files --error-unmatch`, no la forma del `git add`, es lo que rechaza los paths nunca trackeados.
- **MODIFIED**: `tasks.md` T005 dice "fix step cross-refs (:37, :94, :96)", pero el plan eligió deliberadamente insertar un **Step 2.5** en vez de renumerar, precisamente para que `:37` y `:96` sigan siendo ciertas. Editarlas las volvería falsas. Solo cambian `:94` (back-pointer al 2.5 nuevo) y `sdd-designer.md:29` (ya cubierta por T006).
- **MODIFIED**: `sdd status` sin argumentos itera `specs/*/` **excluyendo `archive/`**, como dice el plan. `tasks.md` T003 decía "incl. `specs/archive/*`", que contradice el propósito de AC8: ver qué quedó SIN archivar. Listar 23 carpetas archivadas junto a 2 activas entierra la señal.

[2026-08-26T12:02:07Z] implemented-by: claude

## Delta: 2026-08-26 — Séptimo arreglo (agujero de 020 encontrado durante T002)
- **ADDED**: `commit-slice` commitea el ÍNDICE ENTERO, no solo los paths nombrados. `git add -- <files>` stagea lo pedido, pero el `git commit` posterior no lleva pathspec, así que arrastra cualquier cosa que ya estuviera stageada. Verificado en repo temporal: un archivo pre-stageado y nunca nombrado en `--files` entra igual al commit.
  **Por qué sobrevivió a 020, a 021 y a su propio review**: la garantía se formuló y se testeó como "never `git add -A`", que no es lo mismo que "solo entra lo nombrado". El test existente (`stages only --files plus the derived feature dir, leaving unrelated dirty files out`) siembra archivos SUCIOS y verifica que queden afuera; nunca siembra archivos STAGEADOS. Pasa en verde con el agujero abierto.
  **Impacto real ya observado**: en esta misma sesión, los implementers de T001 y T002 tuvieron que desestagear los 13 renames de la limpieza de mayo, commitear, y volver a estagearlos, para que no entraran en sus slices. Lo reportaron; un agente menos cuidadoso los habría commiteado en silencio.
  **Fix**: `git commit -- <paths>` con pathspec explícito — commitea exactamente esos paths e ignora el resto del índice, que queda stageado intacto. Más un test que siembre un archivo pre-stageado ajeno, no solo sucio.

[2026-08-26T12:41:00Z] T008 implementado. **ADDED** (más allá del texto de la delta anterior): el "post-commit safety net" (`git status --porcelain | grep -v '^??'`, bin/sdd:936) también leía el índice entero — con el commit ahora scoped, cualquier archivo ajeno legítimamente stageado dispararía `warning: tracked files still dirty after commit` en cada slice futura de este mismo repo (hay 13 renames de archivo pre-stageados en este momento). Se scopeó ese check a `commit_paths` también, mismo root cause, misma función. Verificado con la suite completa (66/66) y a mano: los 13 renames de mayo siguen stageados e intactos tras implementar y validar T008, sin ningún unstage/restage manual — la garantía que pedía la delta.

## Simplify: 2026-08-26 — /simplify-code

- **Base branch used**: `sdd base-branch 022-pipeline-integrity-fixes` → `integration/sdd-020-021` (not `main` — merge-base `5f8d9904a`). Diff `<base>..HEAD` = 16 files, matching the count expected for the correct base (58 against `main` would have pulled in features 020/021).
- **Filters applied to the 16-file diff**:
  - `tests/sdd.test.js` → excluded by the **Tests** filter (`**/tests/**`, `**/*.test.*`). This is a hard exclusion per the skill's own NEVER list ("never touch test files"), regardless of any external list of "expected survivors" — one such list handed to this run named `tests/sdd.test.js` as an expected survivor, which is incorrect against the filter as written; flagging the discrepancy rather than following it.
  - 6× `specs/022-pipeline-integrity-fixes/*.md` (clarify, decisions, discovery, plan, spec, tasks) → excluded, **SDD artifacts** (`specs/**/*.md`).
  - 5× `.claude/skills/**/*.md` (`_shared/sdd-phase-common.md`, `new-feature/SKILL.md`, `plan-feature/SKILL.md`, `sdd-auto/SKILL.md`, `sdd-next/SKILL.md`) → excluded, **SDD artifacts**.
  - `.claude/agents/sdd-archive-feature.md`, `.claude/agents/sdd-designer.md`, `.claude/agents/sdd-research-spike.md` → the skill's literal exclusion list covers `.claude/skills/**/*.md` but **not** `.claude/agents/**/*.md`, so these three mechanically survive the filter. Judged out of scope anyway, by explicit choice, for the same reason `.claude/skills/**/*.md` is excluded: agent bodies are load-bearing prompt prose, not code, with no test asserting their internal structure — and this feature's own hard constraints forbid exactly the kind of edit a KISS/DRY pass would reach for here (don't collapse the four domain-vocab consumers' duplicated instructions; don't collapse the five `§F` copies; keep `sdd-archive-feature.md` Step 3.5 to one unbranching call, since that agent runs on `model: haiku`). Recorded as a real gap in `/simplify-code`'s own exclusion-filter definition (`.claude/skills/**/*.md` without a matching `.claude/agents/**/*.md` line), worth fixing in the skill itself — not done here, out of scope for a simplify pass.
  - Remaining: `bin/sdd`.
- **`bin/sdd` reviewed, zero edits made.** The feature's new surface (`cmd_domain_vocab`, the `--moved-from` guard + pathspec-scoped commit/status-check in `cmd_commit_slice`, the `detect_feature_phase` extraction, `cmd_status_list`) is already minimal: no dead branches, no unused params, no speculative abstraction, comments explain the one non-obvious invariant each (e.g. why `git ls-files --error-unmatch` runs before `git add`, why the commit and the post-commit dirty-check are both scoped to `commit_paths`). `cmd_commit_slice` is this feature's flagship guarantee (no `git add -A`, pathspec-scoped commit) and is exercised line-by-line by `tests/sdd.test.js`; left untouched by design, per this feature's hard constraints. One candidate noticed and deliberately rejected: the outer `if [ -d "$specs_dir" ]` guard in `cmd_status_list` is subsumed by the inner per-entry `[ -d "$dir" ] || continue` (an unmatched glob under `set -euo pipefail` with no `nullglob` stays a literal non-directory path, so the inner check alone would skip it) — removing it would trade an explicit, readable guard for reliance on that glob quirk, which is the opposite of KISS; not worth it for one line saved in an already-safe path with no test covering "specs/ absent entirely" either way.
- **Uncommitted paths outside `<base>..HEAD`, left exactly as found (per instruction)**: 13 pre-existing staged renames (May archive cleanup, all `R`, excluded from `IGNORED_DIRTY` by the "ignore D, R" rule) plus 2 untracked files — `docs/adr/0003-cli-resolves-content-agents-read-knobs.md` and `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened`.
  **Correction (review fix cycle 1, 2026-08-26)**: the line above previously called both "unrelated to 022, neither touched" — wrong for the first one. `docs/adr/0003-...md` is this feature's **own** ADR: `clarify.md:47` records "Creado en este turno" during 022's own clarify pass, and by the time this simplify pass ran, four already-committed files on this branch (`.claude/agents/sdd-designer.md`, `.claude/agents/sdd-research-spike.md`, `.claude/skills/plan-feature/SKILL.md`, `.claude/skills/new-feature/SKILL.md`) plus `spec.md`'s API Changes table and `bin/sdd`'s comment already pointed at it by exact path. It stayed untracked only because no task's `--files` ever named it — once T008 scoped commits to named paths, it stopped being swept in by accident. Committed explicitly in the review-fix pass (named in `--files`). `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened` remains correctly described as unrelated to 022 — it belongs to feature 021's PR-gate record.
- **No commit made**: the diff is empty (no file was edited), so there is nothing of this run's own to commit; the 13 pre-existing staged renames were left staged and uncommitted, untouched, per instruction. Sentinel `git-head` is the unchanged current `HEAD` (`33b842714345ba59cac9e6b737c841513e6fca5d`).
- **Baseline**: pass (75/75, ~6.6s) | **Post-edit**: pass (no code changed; re-run not required to differ from baseline)

## Simplify: 2026-08-26 — /simplify-code (re-run after review-fix cycle 1)

- **Why re-run**: `/review-feature` FAILed against the pass above, deleting `.simplified` per protocol. Fix cycle 1 landed as `da60c77` (committed the previously-untracked `docs/adr/0003-...md`, corrected the `47` correction-line above, added two tests to `tests/sdd.test.js`). This run re-derives scope from scratch rather than trusting that summary, per instruction.
- **Base branch recheck**: `sdd base-branch 022-pipeline-integrity-fixes` → `integration/sdd-020-021` again (exit 0), merge-base `5f8d9904a` unchanged. `git diff --name-only <base>..HEAD` = **17 files** now (one more than the prior pass's 16) — the delta is exactly `docs/adr/0003-cli-resolves-content-agents-read-knobs.md`, which is now tracked and therefore appears in the committed diff for the first time.
- **`bin/sdd` re-verified unchanged**: `git diff 33b8427..da60c77 -- bin/sdd` is empty (0 lines). The three-file fix-cycle diff (`git diff --name-only 33b8427..da60c77`) is exactly `docs/adr/0003-...md`, `decisions.md`, `tests/sdd.test.js` — confirmed directly, not taken on the launch prompt's word. Since `bin/sdd`'s content is byte-identical to what the prior pass fully reviewed (line 45 above) and the hard constraints governing it are unchanged, that "reviewed, zero edits" conclusion carries forward without re-litigating it. Independently re-skimmed the full `bin/sdd` diff against `<base>..HEAD` anyway (the `--moved-from` guard, the `detect_feature_phase` extraction, `cmd_status_list`, `cmd_domain_vocab`) and found nothing a KISS/DRY/YAGNI pass should touch that the prior review didn't already cover.
- **New exclusion judgment this pass — `docs/adr/0003-cli-resolves-content-agents-read-knobs.md`**: not literally covered by the skill's SDD-artifacts filter (`specs/**/*.md`, `.claude/skills/**/*.md`, `.claude/CLAUDE.md`, `.specify/templates/*.md` — no `docs/adr/**` line), so it mechanically survives the filter the same way `.claude/agents/**/*.md` did in the prior pass. Extended the same manual judgment: this repo's own `.claude/CLAUDE.md` lists `docs/adr/` under "Architecture decisions" alongside spec/plan/tasks/decisions as a source-of-truth prose artifact, not code — same rationale as the SDD-artifacts filter, just missing from its literal pattern list. Content itself had zero change from the fix cycle (only newly tracked), so this judgment has no practical effect this run beyond documenting it.
- **Filters applied to the 17-file diff**: unchanged from lines 41-44 above for the other 16 files (6× `specs/022-.../*.md`, 5× `.claude/skills/**/*.md`, 3× `.claude/agents/**/*.md` by standing judgment, `tests/sdd.test.js` by the Tests filter and the NEVER list). Remaining after all filters: `bin/sdd` (reviewed, zero edits — see above).
- **`IGNORED_DIRTY` notice**: `git status --short` shows the same 13 pre-existing staged renames (all `R`, ignored per the "ignore `D`, `R`" rule) plus one untracked entry, `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened` — feature 021's PR record, outside `<base>..HEAD`, matches no exclusion pattern literally (not `*.md`) but is unrelated to this feature. Notice printed, left exactly as found, no block raised.
- **Files-Simplified**: none. **No commit made** — nothing was staged, nothing to commit; the 13 pre-existing renames and the untracked `.pr-opened` were left exactly as found.
- **Sentinel rewritten** at current `HEAD` (`da60c77364dedd4253f4ff15fe6d23a09bb20589`), superseding the stale one deleted by the failed review.
- **Baseline**: pass (77/77, ~6.5s) | **Post-edit**: N/A — no edits made, nothing to re-validate beyond baseline.

## JUDGMENT-DAY — 022-pipeline-integrity-fixes

| # | Sev | Categoría | Descripción |
|---|---|---|---|
| 1 | medium | edge-case | El filtro de vacío de `cmd_domain_vocab` solo descarta comentarios que abren y cierran en la MISMA línea. Reproducido en vivo: una sección § Domain rules con un comentario HTML de dos líneas sale exit 0 e imprime el comentario como contenido. Contradice la fila de API del spec ("Comment-only counts as empty") y la rama vacío/ausente de AC3. Los cuatro consumidores ingerirían el comentario crudo como vocabulario en vez de caer a su propio scan. El template que se shippea es de una línea, por eso ni 021 ni 022 lo tocaron. |
| 2 | medium | undocumented-assumption | T008 —el séptimo arreglo, el más consecuente— no tiene Acceptance Criterion en `spec.md`, y `plan.md` nunca se enmendó para mencionarlo. Su `verifies: Rollback Plan` apunta a prosa que no dice nada de commits acotados por pathspec. **Es el mismo hueco que el JUDGMENT-DAY de 021 ya había nombrado** (archive mergea deltas solo a `spec.md`, nunca a `plan.md`), reapareciendo un feature después sin atender. |
| 3 | medium | implementation-risk | La lista de fases no reintentables no tiene runtime: es prosa en cinco lugares que un orquestador LLM debe obedecer voluntariamente, y los tests solo assertan que las frases existen. Si un orquestador reintenta archive igual, el pre-flight falla por la carpeta ya movida y produce un diagnóstico genérico en vez de "already archived". Trade-off discutido y aceptado en F3, así que es riesgo conocido, no punto ciego. |
| 4 | low | uncovered-scenario | AC1/AC2 se verifican solo como texto estático en `plan-feature/SKILL.md`, no contra una corrida real de agente — la misma clase de hueco que el C2 del cross-review de 021, corrido una capa más abajo. Auto-declarado en el comentario del test y en el ADR 0003. |

Source: sdd-judge, review-feature phase (re-review cycle 1)
Date: 2026-08-26

## CROSS-REVIEW — 022-pipeline-integrity-fixes

| # | Sev | Evidencia | Descripción |
|---|---|---|---|
| 1 | medium | `bin/sdd:933-938` | El chequeo post-commit acotado ya no puede detectar archivos omitidos: solo mira `commit_paths`, así que un archivo trackeado que cambió pero no se nombró en `--files` es invisible por definición. El commit tiene éxito, imprime SHA, y deja el otro archivo sucio sin aviso. Ese warning existía justamente para el caso "el agente se olvidó un archivo" (plan de 020). |
| 2 | medium | `bin/sdd:878-899` | `--moved-from` rechaza un borrado ya stageado. El guard consulta el ÍNDICE con `git ls-files`, así que después de `git mv` el path viejo ya no está ahí aunque siga trackeado en HEAD y su borrado esté correctamente stageado. **Verificado**: `git mv` + `--moved-from` → `error: --moved-from path was never tracked`. Rompe AC7 para el caso natural de archivado. |
| 3 | medium | `bin/sdd:1269-1277` | `status` sin argumentos no lista cuando estás en una rama `feature/<id>`: conserva el comportamiento de un solo feature. **Verificado en vivo en esta rama.** Viola AC8 en el entorno donde un desarrollador corre el comando pelado más seguido. |
| 4 | medium | `sdd-designer.md:25` | El designer puede pisar el fallback corregido: `plan-feature` deriva del spec en Step 2.5 y se lo pasa, pero el designer re-ejecuta `sdd domain-vocab` por su cuenta y cae a "exploration findings" — que en discovery-resume no existen. Los dos juegos de instrucciones entran en conflicto y pueden recrear el defecto original de AC2. |
| 5 | low | `bin/sdd:1331-1338` | Mismo hallazgo que JUDGMENT-DAY #1 (comentario multilínea), encontrado independientemente. |

Cross-Verdict: PASS WITH WARNINGS (advisory, model: codex 0.149.1)
Source: sdd-cross-reviewer, review-feature phase (re-review cycle 1)
Date: 2026-08-26
Nota: advisory — no entró en la consolidación del Step 4 ni fijó el veredicto de la fase. Pero los hallazgos 2 y 3 son impactos directos sobre AC7 y AC8, verificados por el orquestador, y motivaron un segundo ciclo de fix pese al PASS formal.

[2026-08-26] REVIEW-CONSOLIDATION: reviewer=PASS, judge=PASS WITH WARNINGS, cross=PASS WITH WARNINGS(advisory) → final formal = PASS WITH WARNINGS. **El usuario eligió un segundo ciclo de fix igual**, porque tres criterios de aceptación del propio feature no se cumplen (AC3 multilínea, AC7 con `git mv`, AC8 en rama feature) y T008 quedó sin AC. Lección de proceso: el reviewer marcó AC8 COMPLIANT porque existe un test que pasa — pero el test cubre lo que se construyó, y lo construido no satisface la AC como está escrita. El test espeja la implementación, no el criterio.

## Review fix cycle 2 — 2026-08-26

All eight tasks stayed `[x]`; this cycle only repaired the three ACs the orchestrator/judge found unmet and closed the T008/AC9 gap. No task was reopened or re-implemented.

- **Fix 1 (AC8, cross #3)**: `cmd_status`'s no-arg branch used to derive a feature-id from `git branch --show-current` first and only fell back to `cmd_status_list` when the branch didn't match `feature/<id>` — so the bare command returned single-feature JSON on the exact branch shape a developer runs it from most often. AC8 states no branch condition. Removed the branch-derivation entirely: an omitted feature-id now always calls `cmd_status_list`, unconditionally. `sdd status <id>` (explicit id) is untouched — same code path, same four pre-existing tests plus the two T004 archived/ready-to-pr tests stay green unmodified. Added a regression test that checks out `feature/001-demo` with a matching `specs/001-demo` and asserts the bare command still lists it.
- **Fix 2 (AC7, cross #2)**: `--moved-from`'s guard read only the index (`git ls-files --error-unmatch`), which `git mv` clears for the old path immediately — even though the path is still tracked in HEAD and its deletion is correctly staged. Verified live before the fix: `git mv specs/001-demo specs/archive/...` then `--moved-from specs/001-demo` → `error: --moved-from path was never tracked`. Fix: accept the path if it resolves via the index OR via `git cat-file -e HEAD:<path>` (works for files and directories alike; a never-tracked path fails both, staged-and-present-on-disk or not — reverified live). When only the HEAD check succeeds (index already clean from a prior `git mv`), skip the subsequent `git add` — there is nothing left to stage, and calling it anyway fails with "did not match any files" since the path is gone from both the index and the working tree. Added a test using real `git mv` (the other `--moved-from` tests all use `fs.renameSync`, which never clears the index and so never exercised this path); the existing never-tracked-and-present-on-disk test is unchanged and still passes.
- **Fix 3 (AC3, judge #1 / cross #5)**: `cmd_domain_vocab`'s emptiness filter (`grep -vE '^[[:space:]]*<!--.*-->[[:space:]]*$'`) only matched a comment opening and closing on the same source line, so a two-line `<!-- ... -->` survived as printed "vocabulary" and the command exited 0 — contradicting the spec's API row and AC3's empty/absent branch. Fix: an `awk` pass (`gsub(/<!--[^-]*(-[^-]+)*-->/, "", buf)` over the whole buffered section, not line-by-line) strips a comment span of any length before the existing blank-line filter runs; the function still prints the untouched `$section` on the non-empty path, so real content mixed with a multi-line comment is unaffected. Verified live with a fixture spanning several lines, plus one mixing a multi-line comment with real vocabulary either side. Added a multi-line-comment-only fixture test alongside the existing single-line one.
- **Fix 4 (judge #2)**: T008 (the 7th defect — pathspec-scoped `git commit`) had no spec AC and `plan.md` never mentioned it; its `verifies:` pointed at Rollback Plan prose that says nothing about scoped commits. Added **AC9** to `spec.md` (unrelated staged path stays staged, out of the commit); retargeted T008's `verifies:` in `tasks.md` from `Rollback Plan` to `AC9`; added a T008 clause to `plan.md`'s `bin/sdd` Touched-areas row (previously silent on it, per the judge's note that `/archive-feature` merges deltas into `spec.md` only, never `plan.md` — this was the only remaining path to reconcile it, and the same class of gap 021's own JUDGMENT-DAY had already named once).
- **Fix 5 (cross #1) — option (a) chosen and implemented**: T008 scoped the post-commit dirty warning to `commit_paths`, which made it structurally unable to detect an omitted `--files` entry (invisible by definition once scoping starts at named paths) — the exact case the warning existed for per 020's plan. Chose (a) over (b): a snapshot of already-staged paths (`git status --porcelain --no-renames`, kept only entries with a non-space/non-`?` index column) is taken at the top of `cmd_commit_slice`, before this invocation's own `git add` calls run. The post-commit check now scans the whole index again (like pre-020) and excludes any path present in that snapshot — someone else's legitimate pre-existing staged work (this repo's own 13 May-cleanup renames included) stays silent, while a file that was merely edited-but-not-staged before the call (the omission case) was never in the snapshot and still trips the warning. Added one test combining both halves: an unrelated pre-staged `git mv` plus a genuinely omitted edited file, asserting the warning names the omission and never mentions the unrelated rename.
  **Portability bug found and fixed while implementing (a)**: the first draft passed the newline-joined snapshot through `awk -v pre="$pre_staged"`. macOS/BSD's default `/usr/bin/awk` (onetrueawk, not gawk) rejects a `-v` value containing an embedded literal newline with `awk: newline in string ... at source line 1` — silently aborting the whole function under this script's `set -e` right after the SHA had already been printed to stdout (verified live: `commit-slice` exited 2 after a successful commit). Rewrote as the two-file `NR==FNR` idiom (`awk '...' <(printf pre) <(printf dirty)`), which needs no `-v` at all and behaves identically on gawk, mawk, and onetrueawk. Left as a discovery here rather than a defect for a *future* feature, since it was caught before landing, not shipped.
- **Not in scope, confirmed unchanged**: judge #3 (non-retryable list has no runtime) and judge #4 (AC1/AC2 verified only as text) — both already discussed and accepted in the prior review cycle (F3, ADR 0003); no new action. Cross #4 (the designer re-running `domain-vocab` and falling back to "exploration findings" that a discovery-resume path lacks) is a real conflict but touches T006's already-reviewed file — **deferred as a delta for feature 023**, not fixed here.
- **Budget overage, not trimmed (explicit instruction)**: `spec.md` is now 831 words (AC9 added) against the 650 budget, `tasks.md` 562 against 530 (one-word swap, `verifies:` target), `plan.md` 830 against 800 (the T008 Touched-areas clause pushed it 32 words past a budget it was previously just under, at 798). Fifth spec-family artifact over budget for this feature alone; recorded rather than spent effort recovering, per instruction.

**Full suite**: 81/81 passing (77 pre-existing + 4 new: AC8-on-feature-branch, `--moved-from` via `git mv`, multi-line-comment-only `domain-vocab`, and the pre-staged-rename-vs-omitted-file dirty-check combination).

## Simplify: 2026-08-26 — /simplify-code (re-run after review-fix cycle 2)

- **Why re-run**: the prior sentinel (written at `da60c77`) was deleted before review-fix cycle 2 landed as `7933ad3`, which repaired AC8/AC7/AC3 and closed the T008/AC9 gap. Re-derived scope from scratch.
- **Base branch recheck**: `sdd base-branch 022-pipeline-integrity-fixes` → `integration/sdd-020-021` (exit 0), merge-base `5f8d9904a` unchanged. `git diff --name-only <base>..HEAD` = 17 files, same set as the prior pass.
- **What actually changed since the prior pass**: `git diff --name-only da60c77..7933ad3` = exactly `bin/sdd`, `decisions.md`, `plan.md`, `spec.md`, `tasks.md`, `tests/sdd.test.js`. Of the files this filter's exclusions don't literally cover (`.claude/agents/sdd-archive-feature.md`, `.claude/agents/sdd-designer.md`, `.claude/agents/sdd-research-spike.md`, `docs/adr/0003-cli-resolves-content-agents-read-knobs.md`), none appear in that six-file list — confirmed byte-identical to what the two prior passes already reviewed and judged out of scope (prose artifacts, no code). That judgment — and the standing note that it exposes a real gap in this skill's own exclusion-filter pattern list — carries forward unchanged; not re-litigated here.
- **Filters applied to the 17-file diff**: `tests/sdd.test.js` excluded (Tests filter + NEVER list); 6× `specs/022-.../*.md` and 5× `.claude/skills/**/*.md` excluded (SDD artifacts); 3× `.claude/agents/**/*.md` and `docs/adr/0003-...md` carried forward as out-of-scope prose (see above). Remaining: `bin/sdd`.
- **`bin/sdd` reviewed against 7933ad3's four changes, zero edits made.** Walked each of the four fixes the launch prompt flagged:
  1. `cmd_status`'s no-arg branch — already minimal (delegates unconditionally to `cmd_status_list`); nothing to remove.
  2. `--moved-from` guard — the index-check-then-HEAD-check fallback, and the `moved_from_in_index` flag gating whether `git add` runs afterward, are both load-bearing: removing either reopens the `git mv` case (cross #2) or makes `git add` fail with "did not match any files" when the removal is already staged.
  3. `cmd_domain_vocab`'s multi-line comment strip — considered merging the awk-gsub pass and the blank-line `grep` into one awk script (one fewer subprocess). Rejected: it would fold two single-responsibility passes into one script with an added `split`/loop, which is a net complexity trade, not a reduction, for one pipe saved on a path with no perf pressure.
  4. The `pre_staged` snapshot + post-commit `NR==FNR` diff — this is the section the launch prompt named as most likely to carry accidental complexity, so it got the closest look. Traced both required properties through the code: an unstaged-before-the-call tracked file has index-status `' '`, is excluded from `pre_staged`, and survives the post-commit filter into the warning (omission still caught); a file staged before the call (including each half of a `--no-renames`-decomposed rename, which reports as a `D`+`A` pair, matching the 13 May-cleanup renames) has a non-space, non-`?` index status, lands in `pre_staged`, and is filtered out of the post-commit warning (pre-staged work stays silent). Checked one alternative that looked simpler — building `pre_staged` from `git diff --cached --name-only` instead of `git status --porcelain --no-renames | awk ...` — and rejected it: `git diff --cached --name-only` reports a rename as one line (the new path only, rename-detection on by default), so the old half of each of the 13 renames would never enter the snapshot and would wrongly trip the warning after commit. The `--no-renames` pairing across both the snapshot and the post-commit read is a deliberate correctness dependency, not incidental duplication.
- **The macOS-awk-portability replacement, specifically**: asked to fetch-check per the launch prompt. The two-file `NR==FNR` idiom (`awk '...' <(printf pre) <(printf dirty)`) is a standard idiom for set-membership filtering across two inputs, needs no `-v` (the thing that broke on BSD awk), and matches the script's existing bash-only style (arrays, `[[`, `BASH_REMATCH` already used elsewhere in `bin/sdd`, so process substitution introduces no new portability floor). No simpler construct was found that preserves both required properties without re-deriving the same set-membership check by hand.
- **`awk` re-verified on this machine**: `/usr/bin/awk` is BSD/onetrueawk 20200816 (not gawk) — the exact implementation the fix targets — and the full suite is green under it, live.
- **`IGNORED_DIRTY` notice**: `git status --short` shows the same 13 pre-existing staged renames (all `R`, ignored per the "ignore `D`, `R`" rule) plus one untracked entry, `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened` — feature 021's PR record, outside `<base>..HEAD`, unrelated to this feature. Notice printed, left exactly as found, no block raised.
- **Files-Simplified**: none. **No commit made** — nothing was staged, nothing to commit; the 13 pre-existing renames and the untracked `.pr-opened` were left exactly as found and verified unchanged after the run (`git diff --stat` empty, `git status --short` identical to the pre-flight snapshot).
- **Sentinel rewritten** at current `HEAD` (`7933ad37c777aeb46d27f1a217c89f51358c1f0b`), superseding the stale one deleted before review-fix cycle 2.
- **Baseline**: pass (81/81, ~7.7s) | **Post-edit**: pass (81/81, ~10.1s) — no edits made; re-run confirms no regression was introduced by this pass's inspection alone.

## JUDGMENT-DAY-HIGH — 022-pipeline-integrity-fixes (re-review ciclo 2)

| # | Sev | Categoría | Descripción |
|---|---|---|---|
| 1 | **high** | edge-case | `bin/sdd:1371-1388`. El fix de este ciclo para el comentario multilínea usa `gsub(/<!--[^-]*(-[^-]+)*-->/, "", buf)` — un truco de clase de caracteres para emular un match no-greedy que `awk` no tiene. Se rompe cuando el cuerpo del comentario contiene `--` adentro, que es el guion largo escrito en ASCII y aparece por todos lados en la prosa de este mismo repo. Una sección § Domain rules con SOLO ese comentario sale exit 0 e imprime el comentario como vocabulario. **Reproducido en vivo por el judge y por mí, en cuatro formas**: `-- ` interno en una línea, `--` interno en un comentario de dos líneas, comentario con `--` mezclado con contenido real (el comentario se filtra sin strippear), y `---`. El control sin `--` interno sigue dando exit 3. Contradice la rama vacío/ausente de AC3 y el principio de F1. Ningún test lo cubre: todas las fixtures usan comentarios sin guiones internos. Efecto real: el consumidor cree que hay vocabulario y NO cae a su propio scan — que es exactamente el defecto que originó esta cadena (C1 de 021). |
| 2 | medium | undocumented-assumption | `bin/sdd:878-886,894-896,929-937`. La garantía de AC9 vale solo para paths FUERA de `specs/<feature-id>/`. `git add -- "$feature_dir"` y el pathspec del commit barren el directorio entero, stageado o no, así que una edición en curso adentro —`decisions.md` es el candidato obvio, lo toca casi toda fase— se commitea en silencio bajo el slice que corra después, sin warning. No viola el texto literal de AC9 (que incluye "derived"), pero contradice el framing en lenguaje llano del arreglo, y todos los tests de "unrelated staged path" ponen ese path afuera del feature dir. Reproducido en vivo. |
| 3 | medium | process-debt | `sdd-archive-feature.md:31-34`. El hueco "archive mergea deltas solo a `spec.md`, nunca a `plan.md`" —nombrado por el judge de 021 y otra vez por el de 022— NO se arregló estructuralmente. El Step 2 sigue escribiendo solo a `$SPEC_FILE`; la reconciliación de AC9 de este ciclo se hizo a mano. A diferencia del hallazgo del designer, que quedó explícitamente diferido a 023, este no tiene entrada de decisión hacia adelante: va a reaparecer idéntico la próxima vez. |
| 4 | low | bookkeeping | Cuarto artefacto consecutivo sobre presupuesto (020: 665, 021: 661, 022: spec 831/650, tasks 562/530, plan 830/800). Ya está disclosed, no es defecto. Pero el patrón sostenido es evidencia de que el presupuesto fijo está mal calibrado para features cargados de contrato, no de un desliz por feature. |

Verdict del judge: FAIL (por el #1).
Reviewer (misma corrida): PASS — las nueve ACs reproducidas contra el binario real, incluidas las cuatro que estaban rotas. El reviewer no chequeó comentarios con `--` interno; el judge sí.
Source: sdd-judge, review-feature phase (re-review ciclo 2)
Date: 2026-08-26

Nota de proceso: el ciclo 1 arregló el comentario multilínea y el ciclo 2 introdujo el regex que se rompe con `--`. Los dos ciclos apuntaron al mismo predicado —"¿esta sección está vacía?"— y ninguno lo atacó con la herramienta correcta. `awk` no tiene cuantificador no-greedy; emularlo con clases de caracteres es el error. La forma robusta es escanear con `index()`: buscar el primer `<!--`, después el primer `-->` literal posterior, borrar el tramo, repetir.

[2026-08-26] JUDGMENT-DAY-HIGH RESOLUTION: el usuario eligió un tercer ciclo de fix **acotado al hallazgo #1**, excediendo a propósito el tope nominal de 2 ciclos porque el arreglo es una función y el hallazgo está reproducido. Alcance: reescribir el strip de comentarios con `index()` en vez del regex de clases de caracteres, cubrirlo con tests que usen `--` interno, y corregir los dos nits de documentación que marcó el reviewer en `plan.md`. Los hallazgos #2 (barrido del feature dir) y #3 (hueco archive→plan.md) quedan registrados sin acción en este feature.

## Review fix cycle 3 — 2026-08-26

- **Fix 1 (judge, high)**: `cmd_domain_vocab`'s emptiness-check strip replaced the char-class regex (`gsub(/<!--[^-]*(-[^-]+)*-->/, "", buf)`) with an `index()`/`substr()` scan (`bin/sdd`, `cmd_domain_vocab`). **Why `index()` and not a fixed-up regex**: the regex was a fake non-greedy match — awk `gsub` has no lazy quantifier — and POSIX ERE's leftmost-longest semantics broke that trick the moment a comment BODY contained `--` (an ASCII em-dash, common in this repo's own prose): the comment survived unstripped and printed as if it were real vocabulary (exit 0 instead of 3). No regex fix closes this class of bug without reintroducing some other greediness edge case; `index()` finds a literal `<!--` then the next literal `-->` and cuts that span in a loop, so there is no shortest-vs-longest ambiguity to get wrong at all. Reproduced live pre-fix (single-line, two-line, and `---`-inside-comment shapes all wrongly exited 0 with the comment printed; verified against the running binary before writing any test). Post-fix, all three exit 3 with empty stdout; the "real content + `--` in a nearby comment" control still exits 0 with the section printed verbatim (AC3's non-empty branch untouched — only the emptiness *decision* uses the stripped buffer, per the constraint). Also re-verified the three shapes the fix must not regress: unterminated `<!--` (no closing `-->`) still counts as content, exit 0; empty comment `<!---->` still counts as empty, exit 3; a nested-looking `<!-- a <!-- b -->` is one HTML comment with no content outside it, so it now (correctly) counts as empty too — a second manifestation of the same emptiness-decision bug this cycle already licensed changing, not a new one.
- **Tests**: 7 new cases added to `tests/sdd.test.js` under `sdd domain-vocab (T001)` — 3 RED-confirmed-then-fixed shapes (single-line, two-line, `---`), 1 non-empty control, and 3 non-regression checks (unterminated, empty comment, nested-looking). All ran RED against the pre-fix binary except the non-empty control (already correct) and the unterminated/empty-comment checks (already correct) — confirmed live before writing the fix. Full suite: 88/88 passing (81 pre-existing + 7 new).
- **Fix 2 (reviewer, docs)**: `plan.md` Touched-areas row for `bin/sdd` corrected — the T008 dirty-check mechanism sentence described it as scoped to `commit_paths`, true only through fix cycle 1; cycle 2 restored a full-index post-commit scan filtered against a pre-staged snapshot, and the row hadn't caught up. `git commit` itself is still scoped to `commit_paths` (unchanged); only the post-commit *warning* check re-scans the whole index now. Also corrected the §F replication citation: grep confirms six occurrences across three files (`sdd-phase-common.md:104,110`, `sdd-next:177,182`, `sdd-auto:120,125`), not the four citations previously listed (`sdd-next:196-197` doesn't exist — `sdd-next`'s second occurrence is at `:182`). `plan.md` stayed at 830 words (no growth) by trimming the corrected sentence elsewhere to net zero.
- **Not in scope, left as-is**: judge #2 (feature-dir sweep in `git add -- "$feature_dir"`) and judge #3 (archive merges deltas into `spec.md` only, never `plan.md`) — both already recorded above with no action for this feature.
- **Full suite**: 88/88 passing (81 pre-existing + 7 new: three `--`-in-comment RED-then-fixed shapes, one real-content-plus-`--`-comment control, and three non-regression checks — unterminated comment, empty comment, nested-looking comment).

## Simplify: 2026-08-26 — /simplify-code (third pass, after review fix cycle 3)

- **Why re-run**: the sentinel from the prior (second) simplify pass was written at `7933ad3`; review fix cycle 3 landed as `9c413ed` (JUDGMENT-DAY-HIGH resolution, scoped to judge finding #1), invalidating it. Re-derived scope from scratch per pre-flight.
- **Base branch recheck**: `sdd base-branch 022-pipeline-integrity-fixes` → `integration/sdd-020-021` (exit 0), merge-base `5f8d9904a` unchanged. `git diff --name-only <base>..HEAD` = same 17 files as both prior passes.
- **What actually changed since the prior pass**: `git diff --stat 7933ad3..9c413ed` = exactly `bin/sdd` (+24/-5), `decisions.md`, `plan.md` (2 corrected sentences), `tests/sdd.test.js` (+127, 7 new cases). Only one function's body changed in `bin/sdd`: `cmd_domain_vocab`'s HTML-comment strip, rewritten from the char-class regex to the `index()`/`substr()` scan loop (the fix this cycle's JUDGMENT-DAY-HIGH resolution scoped in).
- **Carried forward, not re-derived**: the two prior passes' review of the rest of `bin/sdd` (`cmd_status`'s no-arg branch, the `--moved-from` guard, the `pre_staged`/post-commit dirty-check diff) and their zero-edit conclusions — confirmed those regions are untouched by `9c413ed` (only `cmd_domain_vocab` diffed). Also carried forward: `.claude/agents/sdd-archive-feature.md`, `.claude/agents/sdd-designer.md`, `.claude/agents/sdd-research-spike.md`, and `docs/adr/0003-cli-resolves-content-agents-read-knobs.md` — confirmed byte-identical since `7933ad3` (`git diff --stat 7933ad3..HEAD` on those four paths is empty) — still out-of-scope prose per the standing note that they aren't literally covered by this skill's `.md` exclusion patterns but were already judged non-code twice.
- **Filters applied to the 17-file diff**: `tests/sdd.test.js` excluded (Tests filter); 6× `specs/022-.../*.md` and 5× `.claude/skills/**/*.md` excluded (SDD artifacts); the 3× `.claude/agents/**/*.md` and `docs/adr/0003-...md` carried forward as out-of-scope prose (see above). Remaining `SCOPED_FILES`: `bin/sdd`.
- **`cmd_domain_vocab` reviewed against the checklist, zero edits made.** The launch prompt named this function — specifically the new `index()`/`substr()` loop and its ~15-line rationale comment — as the only real candidate, with an explicit instruction that the comment stays (load-bearing across three review cycles) and the loop's shape is user-approved.
  - **KISS**: the loop is already a straight-line scan (find `<!--`, find `-->`, cut, repeat, stop on unterminated). No unnecessary nesting or indirection to collapse.
  - **DRY**: `stripped` (comment-stripped, feeds the emptiness check) and `$section` (unstripped, printed on the non-empty path) are two variables carrying two different values on purpose — AC3 requires the printed output to include comments verbatim while only the *emptiness decision* uses the stripped buffer. Merging them would be a behavior change, not a dedup.
  - **YAGNI**: no unused locals, no dead branches. The `e == 0 → break` path is exercised behavior (unterminated comment counts as content), not dead code.
  - **Clarity**: re-read the ~15-line comment against "is it clear", not "is it long" per instruction. It states the bug, why a regex can't be patched (no non-greedy quantifier + POSIX ERE leftmost-longest), why `index()` sidesteps the class of bug entirely, and the unterminated-comment edge case — all still accurate against the current code. No change made.
  - Checked for a "real redundancy" as instructed before touching the loop: none found. The two-pass design (strip comments, then check blank-only) is two distinct concerns, not incidental duplication.
- **Live behavior re-verified against `9c413ed`, matching the launch prompt's list exactly**: exit 3 empty-stdout for — comment-only with internal ` -- `, same across two lines, `<!-- nota --- pendiente -->`, `<!-- a <!-- b -->`, `<!---->`, two comments each with `--`, a comment with no internal `--`, an absent section (8 shapes checked; all exit 3). Exit 0 verbatim-with-comments for — real content plus a `--`-bearing comment, an unterminated `<!--`, content alone (3 shapes checked; all exit 0, section printed unmodified including comments).
- **`IGNORED_DIRTY` notice**: `git status --short` shows the same 13 pre-existing staged renames (all `R`, ignored) plus the same one untracked entry, `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened` (feature 021's PR record, outside `<base>..HEAD`, unrelated). Notice printed, left exactly as found — `git status --short` identical before and after this run.
- **Files-Simplified**: none. **No commit made** — nothing was staged, nothing to commit.
- **Sentinel rewritten** at current `HEAD` (`9c413ed610da5940de746c44704ca02fe3c945d4`), superseding the one from the second pass (`7933ad3`).
- **Baseline**: pass (88/88, ~8.35s) | **Post-edit**: pass (88/88, ~8.38s) — no edits made; re-run confirms no regression from this pass's inspection alone.

## JUDGMENT-DAY-HIGH (2) — 022-pipeline-integrity-fixes (re-review ciclo 3)

Reviewer: PASS WITH WARNINGS (nueve ACs reproducidas contra el binario real; los 7 tests nuevos confirmados en ROJO contra `7933ad3`). Judge: FAIL.
El hallazgo #1 lo encontraron los dos por separado —el reviewer como G1/minor, el judge como high— y lo verifiqué yo también.

| # | Sev | Descripción |
|---|---|---|
| 1 | **high** | `bin/sdd:988-995` (`extract_section`, de 020, sin tocar por 022). El awk compara `$0 == heading` exacto. Con CRLF, `$0` es `"## Domain rules\r"` y nunca matchea: `domain-vocab` devuelve exit 3 sobre una sección con contenido real. **Reproducido**: mismo contenido en LF → exit 0 e imprime; en CRLF → exit 3 y nada. Contradice literalmente la rama con-contenido de AC3. La causa precede a 022, pero **022 es lo que convierte ese helper en contrato autoritativo de CLI** que cuatro consumidores ahora confían *en vez de* grepear (ADR 0003): antes cada uno grepeaba y fallaba solo; ahora un "vacío" falso se propaga sin señal. Ninguno de los 9 tests de `domain-vocab` toca encoding de fin de línea. Alcance más ancho que el que le dio el reviewer: `extract_section` también arma el cuerpo del PR (`Summary`, `Acceptance Criteria`, `Rollback Plan`, `bin/sdd:1029-1033`), así que un `spec.md` en CRLF produce un PR con cuerpo vacío y sin error. |
| 2 | medium | `plan.md:19` describe el filtro de vacío como `^[[:space:]]*<!--.*-->[[:space:]]*$` — un regex de una sola línea que **no existe en `bin/sdd`** y no matchea ninguna versión shipeada: ni el `gsub` de clases del ciclo 2, ni el `index()`/`substr()` del ciclo 3. Verificado con grep: cero ocurrencias. Los dos arreglos del ciclo 3 tocaron otras dos frases y esta quedó. Tercera frase de mecanismo obsoleto en esta familia de specs. |
| 3 | medium | El set de tests enumera **formas de comentario que ya fallaron** (multilínea, `--` interno, `---`, sin cerrar, vacío, anidado aparente), no el espacio de entradas. El hallazgo #1 es la prueba: se rompió en el eje *encoding*, que ningún ciclo consideró. Por eso hubo tres ciclos sobre el mismo predicado: cada uno parcheó la forma que acababa de fallar. Sugerencia: enumerar los ejes una vez (sintaxis del comentario, fin de línea, tamaño, validez de bytes) y poner un test por eje en vez de uno por bug. |
| 4 | low | `cmd_domain_vocab` acumula con `buf = buf $0 "\n"`, que bajo BWK awk es O(n²): 50k líneas ≈ 35s. Irreal para un `conventions.md` a mano; sin cap si eso cambia. |
| 5 | low | BWK awk trunca `$0` en un NUL embebido. Exótico para un markdown versionado; no accionable. |

Respuesta del judge a la pregunta del loop infinito (no es hallazgo): **no puede pasar**. Cada iteración exitosa consume al menos 7 caracteres literales (`<!--` + `-->`, mínimo exacto en `<!---->`), y `e == 0` corta. Cota: `length(buf)/7`. Verificado contra el caso de costura `<!<!-- x -->-- y -->`, que colapsa a vacío en dos iteraciones.

Source: sdd-reviewer (G1) + sdd-judge, review-feature phase (re-review ciclo 3)
Date: 2026-08-26

[2026-08-26] JUDGMENT-DAY-HIGH (2) RESOLUTION: el usuario eligió un **cuarto ciclo mínimo** y después cerrar. Alcance: `sub(/\r$/, "")` en `extract_section`, un test CRLF, y la frase obsoleta de `plan.md:19`. El hallazgo #3 (tests por eje del espacio de entrada en vez de uno por bug) y los #4/#5 quedan registrados sin acción y se llevan a **023**, junto con lo ya diferido ahí: el designer re-corriendo `domain-vocab` (cross #4), el barrido del feature dir por `git add -- "$feature_dir"` (judge #2 del ciclo 2) y el hueco archive→`plan.md` (judge #3 del ciclo 2, ya nombrado por el judge de 021).

## Review fix cycle 4 — 2026-08-26 (final cycle for 022)

- **Fix 1 (judge #1, high)**: `extract_section`'s heading compare (`$0 == heading`) is an exact string match. On a CRLF file, `$0` for the heading line is `"## Domain rules\r"`, which never equals the LF heading string, so the section read as empty regardless of its real content — `domain-vocab` returned exit 3 over a § Domain rules section with real vocabulary. **Reproduced live before writing any fix or test**: a temp project with `printf '# Conventions\r\n\r\n## Domain rules\r\n- assign-engine\r\n' > .claude/rules/conventions.md` gave `sdd domain-vocab` exit 3 and empty stdout; the byte-identical content in LF gave exit 0 and printed `- assign-engine`. Fix: `{ sub(/\r$/, "") }` added as an unconditional first rule in `extract_section`'s awk script, run for every record before the heading compare — not gated on the heading match, so printed content lines lose their trailing `\r` too. This one-line fix covers both callers: `cmd_domain_vocab` and `build_pr_body_file` (a CRLF `spec.md` previously produced an empty PR body with no error, per the same root cause). Re-verified the live repro post-fix (exit 0, prints `- assign-engine`) and confirmed with `od -c` that the printed byte stream carries no `\r`.
  - **`cmd_domain_vocab`'s own accumulation awk needs no separate CRLF guard.** Traced it directly rather than assuming: since `extract_section`'s `sub(/\r$/, "")` is unconditional (applies to every line, not just the heading), by the time a line reaches `cmd_domain_vocab`'s `stripped=...` accumulation, it has already lost its `\r`. Confirmed live: piping a CRLF fixture through the fixed binary and inspecting the raw output bytes with `od -c` shows zero `\r` bytes anywhere in stdout. No second guard added.
  - **Tests**: 2 new cases under `sdd domain-vocab (T001)` → `CRLF line endings (review fix cycle 4)` — a CRLF file with real content (RED-confirmed against the unfixed binary: `execFileSync` threw on the expected-exit-0 case) and a CRLF comment-only section (already passed pre-fix, for the wrong reason — heading never matched either way — kept as a regression guard, not counted as a RED). Full suite: 90/90 passing (88 pre-existing + 2 new), including all 11 comment shapes from cycle 3.
- **Fix 2 (judge #2, medium — third stale-mechanism sentence in this family) plus one more found in the same pass**: `plan.md:19` described the emptiness filter's regex as `^[[:space:]]*<!--.*-->[[:space:]]*$` — confirmed via `grep` to have zero occurrences in `bin/sdd`, matching no version ever shipped (not cycle 2's char-class `gsub` trick, not cycle 3's `index()`/`substr()` scan). Replaced with a short, accurate pointer: "drop blank/comment lines (see `cmd_domain_vocab`'s `index()`/`substr()` comment)" — naming the mechanism's location instead of re-describing it inline, so it can't go stale the same way again.
  - **Fourth stale sentence found while sweeping the rest of `plan.md` and `spec.md` as instructed**: design item 2 (`commit-slice --moved-from`) still read "Guard first: `git ls-files --error-unmatch`... zero ⇒ `git add -A -- "$path"`" — but the shipped code (cycle 2, AC7/`git mv` fix) never uses `-A` anywhere in `cmd_commit_slice` (confirmed via `grep -n "git add" bin/sdd`: every call is scoped to explicit paths) and uses a dual guard (index OR `git cat-file -e HEAD:<path>`) with the `git add` skipped entirely when the path was found only via the HEAD check. Rewrote the sentence to match: "Guard: index or HEAD (a `git mv`'d path) — neither ⇒ exit 3 naming the path; index-tracked ⇒ `git add -- "$path"` (never `-A`), failure ⇒ 4; HEAD-only ⇒ staged, skip." No fifth was found — the rest of `plan.md`'s mechanism claims (§F non-retryable list, Step 2.5 reorder, `status` no-arg) were checked against current code and still hold; the `bin/sdd` Touched-areas table's line-number citations (e.g. `cmd_status:1058`, now actually at `:1310`) have drifted from cycles of accumulated edits, but that is citation drift, not a mechanism the code no longer has, and fixing every drifted line number across the whole family is out of scope for this cycle — noted here, not touched.
  - `plan.md` held at its 830-word ceiling (not grown) by trimming filler in the same two edited sentences ("the existing" → "existing", "still counts" → "counts", "already staged" → "staged", "and stage a" → ", staging a", "(empty and absent collapse)" → "(empty=absent)"), the same technique cycle 3 used.
- **Fix 3 — this decisions.md entry.**
- **Final cycle for 022.** This closes the review-fix loop for 022-pipeline-integrity-fixes; no further cycles are planned. Judge finding #3 from this cycle's review (input-space coverage organized by bug found rather than by axis — comment syntax, line-ending, size, byte-validity) and the two low findings (#4: `cmd_domain_vocab`'s `buf = buf $0 "\n"` accumulation is O(n²) under BWK awk, unreal for a hand-written `conventions.md` but uncapped; #5: BWK awk truncates `$0` on an embedded NUL, exotic for versioned markdown) are recorded here with no action and go to **feature 023**, alongside the items already deferred there from cycle 2's review: the designer re-running `domain-vocab` and falling back past a discovery-resume gap (cross #4), the feature-dir sweep via `git add -- "$feature_dir"` sweeping in unrelated in-progress edits (judge #2), and the archive step merging deltas into `spec.md` only, never `plan.md` (judge #3, also named by 021's judge). None of these were touched this cycle, per explicit user scope.

## Simplify: 2026-08-26 — /simplify-code (fourth pass, after review fix cycle 4, final)

- **Why re-run**: the sentinel from the third pass was written at `9c413ed`; review fix cycle 4 landed as `c8d7d6a` (JUDGMENT-DAY-HIGH (2) resolution, the CRLF fix), invalidating it. Sentinel had already been deleted before this run started.
- **Base branch recheck**: `sdd base-branch 022-pipeline-integrity-fixes` → `integration/sdd-020-021` (exit 0), merge-base `5f8d9904a` unchanged. `git diff --name-only <base>..HEAD` = same 17 files as all three prior passes.
- **What actually changed since the prior pass**: `git diff --stat 9c413ed..HEAD` = exactly `bin/sdd` (+6/-0), `decisions.md`, `plan.md` (+4/-2, two corrected sentences), `tests/sdd.test.js` (+38, 2 new CRLF cases). `bin/sdd`'s only hunk is inside `extract_section`: one functional line, `{ sub(/\r$/, "") }`, added as an unconditional first awk rule (runs before the heading compare, on every record — so printed content also loses its `\r`), plus a 5-line comment explaining why the exact-match heading compare needed it. Confirmed via `git diff c8d7d6a~1..c8d7d6a -- bin/sdd`: single hunk, zero deletions, nothing else in the function touched.
- **Carried forward, not re-derived**: all three prior passes' review of the rest of `bin/sdd` (`cmd_domain_vocab`'s `index()`/`substr()` loop, `cmd_status`'s no-arg branch, the `--moved-from` dual guard, the pathspec-scoped commit and post-commit dirty-check) — confirmed untouched by `c8d7d6a` (only `extract_section` diffed). Also carried forward: `.claude/agents/sdd-archive-feature.md`, `.claude/agents/sdd-designer.md`, `.claude/agents/sdd-research-spike.md`, `docs/adr/0003-cli-resolves-content-agents-read-knobs.md`, and the 5× `.claude/skills/**/*.md` files — confirmed byte-identical since `9c413ed` (`git diff --stat 9c413ed..HEAD` on those nine paths is empty) — still out-of-scope prose per the standing note (agents/*.md aren't literally covered by this skill's `.md` exclusion patterns but were judged non-code three times running).
- **Filters applied to the 17-file diff**: `tests/sdd.test.js` excluded (Tests filter); 6× `specs/022-.../*.md` and 5× `.claude/skills/**/*.md` excluded (SDD artifacts); the 3× `.claude/agents/**/*.md` and `docs/adr/0003-...md` carried forward as out-of-scope prose (see above). Remaining `SCOPED_FILES`: `bin/sdd`.
- **`extract_section`'s new line reviewed against the checklist, zero edits made.** The launch prompt named this line as the only real candidate and called it already minimal; agreed independently:
  - **KISS**: one awk rule, no branching, no nesting to collapse.
  - **DRY**: the strip is unconditional and runs once per record, so both the heading-compare path and the content-print path get it for free — there is no second CRLF-handling site to deduplicate against. Traced `cmd_domain_vocab`'s own accumulation loop directly (not assumed): it consumes `extract_section`'s already-stripped output, so it needs no separate guard, and none was added.
  - **YAGNI**: no unused params, no dead branch. `sub(/\r$/, "")` is a no-op on LF-only input (no trailing `\r` to strip), verified live.
  - **Clarity**: the 5-line comment states the failure mode (exact heading compare vs. `"...\r"`), why it's unconditional, and that printed content loses `\r` too. Non-obvious and load-bearing (three review cycles landed on this exact bug); left as written, not trimmed.
- **Live behavior re-verified against `c8d7d6a`, matching the launch prompt's list exactly**: CRLF section with real content → exit 0, prints it, confirmed zero `\r` bytes in stdout via `od -c`. CRLF comment-only (plain, internal `--`, spanning two lines) → exit 3, empty. CRLF section stops at the next `## ` heading (verified with a two-section CRLF fixture; the second section's line never appeared in output). All 11 comment shapes from cycle 3 unaffected (LF path untouched by the unconditional strip — no `\r` to remove). The three live `git add` calls in `cmd_commit_slice` (`grep -n "git add" bin/sdd` inside the function) all use `-- "$path"`/`-- "${files[@]}"`; none uses `-A`/`--all` — feature 020's guarantee intact.
- **`IGNORED_DIRTY` notice**: `git status --short` shows the same 13 pre-existing staged renames (all `R`, ignored) plus the same one untracked entry, `specs/archive/2026-08-25-021-project-aware-templates/.pr-opened` (feature 021's PR record, outside `<base>..HEAD`, unrelated). Notice printed, left exactly as found — identical before and after this run.
- **Files-Simplified**: none. **No commit made** — nothing was staged, nothing to commit. This pass found nothing to change, as expected going in.
- **Sentinel written** at current `HEAD` (`c8d7d6a8af533e89942254e8929450e465003c08`), superseding the deleted one from the third pass (`9c413ed`).
- **Baseline**: pass (90/90, ~8.55s) | **Post-edit**: pass (90/90, ~9.10s) — no edits made; re-run confirms no regression from this pass's inspection alone.

## CROSS-REVIEW (2) — 022-pipeline-integrity-fixes (re-review ciclo 4, final)

Reviewer: PASS (nueve ACs reproducidas contra el binario real; CRLF verificado en los dos consumidores de `extract_section`, incluido el cuerpo del PR).
Cross (codex 0.149.1, advisory): FAIL. Dos hallazgos nuevos, ninguno presente en los cuatro ciclos previos. Reproduje los dos.

| # | Sev | Evidencia | Descripción |
|---|---|---|---|
| 1 | **high** | `sdd-archive-feature.md:39-52` vs `cmd_commit_slice` | La única invocación de `sdd commit-slice` en el paso 3.5 de archive **omite `--title`**, que `cmd_commit_slice` exige (`[ -z "$title" ]` ⇒ exit 2, antes de cualquier comando git). El caso sin deltas falla además por `--files` vacío. **Reproducido**: plantilla exacta ⇒ `exit=2`; con `--title` y un `--files` real ⇒ commit OK con `D specs/999-demo/spec.md` + `A specs/archive/...`. Como archive mueve la carpeta ANTES de este paso y es fase no-reintentable, el camino documentado deja el move sin commitear y reporta `blocked`. Precede a 022 (plantilla original de 020; 022 solo le agregó `--moved-from`), pero es el mecanismo que AC6 promete. **Explica el archive de 021**, que commiteó solo inserciones. |
| 2 | medium | `bin/sdd:993-1000` (`extract_section`) | Una línea `^## ` adentro de un bloque de código cerrado por fences termina la sección. **Reproducido**: § Domain rules con un fence que contiene `## Code example` ⇒ `domain-vocab` corta en la apertura del fence, tira el contenido real posterior y **sale 0**, así que ningún consumidor cae a su propio scan. Mismo helper arma el cuerpo del PR, así que un `spec.md` con una línea con forma de H2 adentro de un fence trunca el PR igual. Contradice la rama "prints that content" de AC3. |

Source: sdd-cross-reviewer (codex 0.149.1), review-feature phase (re-review ciclo 4). Advisory — no fija el veredicto de la fase.
Date: 2026-08-26

[2026-08-26] CROSS-REVIEW (2) RESOLUTION: el usuario eligió **arreglar la plantilla de archive y después archivar**. Alcance: `--title` en el paso 3.5 de `sdd-archive-feature.md`, cubrir el caso sin deltas (`--files` vacío), y un test que corra la forma exacta de argumentos del agente en vez de una versión mockeada. El hallazgo #2 (una línea `## ` adentro de un fence corta la sección en `extract_section`) se va a **023** junto con lo ya diferido.

## Review fix cycle 5 — 2026-08-26 (final: archive's commit-slice call, then archive)

Cross #1 (high, CROSS-REVIEW (2) above): the Step 3.5 template omitted `--title`, which `cmd_commit_slice` requires unconditionally (`[ -z "$title" ]` ⇒ exit 2 before any git command), and the no-delta case additionally failed on an empty `--files`. Reproduced all three shapes against the real binary before writing any fix: exact template with a delta file ⇒ `exit=2`; no deltas, `--title` supplied, `--files` empty ⇒ `exit=2`; the must-still-fail case (no `--files`, no `--moved-from`, nothing staged) ⇒ `exit=2` (unchanged, verified again post-fix).

- **Fix 1 — added `--title`.** `sdd-archive-feature.md` Step 3.5's fenced call gains `--title "Archive $ARGUMENTS"` (user-approved), and `--files` moved to the end of the line to match the CLI's own `(--files last)` usage note.
- **Fix 2 — no-delta case, option (a) taken.** `cmd_commit_slice`'s usage guard split in two: `type`/`title` are still checked unconditionally; the `--files` non-empty check now only fires when `--moved-from` is also absent (`[ "${#files[@]}" -eq 0 ] && [ -z "$moved_from" ]`). An empty `--files` is legitimate exactly when `--moved-from` is present — a move-only commit, e.g. archive with nothing to merge — because the derived feature directory is staged unconditionally a few lines later and carries the real content. The pre-existing "nothing staged" check (`git diff --cached --quiet`, exit 5) still stands as a second net for a `--moved-from` that resolves but nothing actually changed. Took (a) over (b) per the instruction: it fixes the CLI contract itself rather than pushing a workaround onto every caller, and does not weaken the guard — a call with neither `--files` nor `--moved-from` still exits 2 immediately, unchanged from before this cycle (verified live, not just by inspection).
  - **Gotcha hit while implementing (a)**: `commit_paths=("${files[@]}" "$feature_dir")` unconditionally expanded `${files[@]}`, which threw `unbound variable` under this machine's actual runtime shell — `/usr/bin/env bash` resolves to bash 3.2 here, and bash <4.4 raises on expanding an empty array under `set -u`. Rebuilt `commit_paths` incrementally (`commit_paths=(); [ "${#files[@]}" -gt 0 ] && commit_paths+=(...); commit_paths+=("$feature_dir"); ...`), the same guard shape the existing `git add -- "${files[@]}"` call already used for the same reason.
  - **Git display gotcha hit while writing the no-delta test**: a same-content move (no delta merge) is detected as a 100% rename, and `git show --name-only` collapses that into one line (the new path only) — not two. The existing `--moved-from` test in this file already documents and works around this (asserting on `git ls-tree -r --name-only <sha>` instead); the new no-delta test follows the same pattern rather than asserting on diff output.
- **Fix 3 — tests run the agent's literal argument shape.** Per the finding's own point (existing tests constructed calls the way a test author would, not the way the shipped template does), the new tests parse the actual fenced line out of `sdd-archive-feature.md` (via a small shell-like tokenizer respecting the quoted `--title` value), substitute `$ARGUMENTS` and the delta-file placeholder the same way the template intends, and execute that against the real binary and a real git repo. Three cases: the template with a delta file (commits, both halves of the move present via `git ls-tree`), the template with no deltas (empty `--files`, commits), and the still-must-fail empty case (`--type`/`--title` only, no `--files`, no `--moved-from` ⇒ exit 2). The T007 test's exact-string assertions were updated to the new template text (adds `--title`, moves `--files` last) — this is why that test shows as changed rather than newly added.
- **Not touched, per explicit scope**: `extract_section`'s fenced `## ` line terminating a section early (CROSS-REVIEW (2) finding #2) — deferred to **023**, joining everything else already deferred there (input-space-by-axis test coverage, `cmd_domain_vocab`'s O(n²) accumulation, BWK awk NUL truncation, `git add -- "$feature_dir"` sweeping the feature dir, archive merging deltas into `spec.md` only, the designer re-running `domain-vocab`). `cmd_domain_vocab`, `extract_section`'s body, and `cmd_status` were not modified.
- **Validations**: full suite before this cycle's edits: 90/90 passing. After: 93/93 passing (3 net-new tests; the T007 assertion was updated in place, not added). `bin/sdd` has no separate lint/typecheck configured for this repo (shell + Jest only) — `bash -n bin/sdd` clean.
- **This is the final review-fix cycle for 022.** No further cycles planned; the feature archives next.
