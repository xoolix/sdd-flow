# Model Overrides

<!-- Project-specific model routing overrides. Claude Code loads this file
     automatically via the .claude/rules/*.md convention — no explicit
     reference needed from CLAUDE.md.

     The default model routing lives in .claude/CLAUDE.md §Model Routing
     (symlinked from SDD_HOME). To override for this project only, add
     rows below with the same columns:

     | Role | Skill / Context | Model |
     |------|----------------|-------|
     | Implementation | implement-task | opus |

     The orchestrator reads this file and applies any overrides before
     falling back to the default table. -->
